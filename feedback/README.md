# Local Feedback Notes

This directory is for local agent feedback notes created by:

```bash
npm run feedback:note -- --type gap --title "Short title"
```

Individual `feedback/*.md` notes are ignored by git because they may contain
private target context. Safe, generalized feedback should be converted into a
GitHub issue with the templates in `.github/ISSUE_TEMPLATE/`.

