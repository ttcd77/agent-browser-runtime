// register-aliases.mjs — alias registrations.
// F4 (2026-06-11): All 94 devtools_* aliases and the browser_fill alias have been removed.
// Callers now use canonical browser_* / profile_* tool names directly.
export function registerAliases(_deps) {
  // No aliases registered.
}
