[
  {
    "description": "Agent-facing F12 router. Pick a focus and get the right DevTools evidence without choosing from dozens of low-level tools.",
    "name": "agent_inspect",
    "parameters": {
      "properties": {
        "focus": {
          "description": "F12 panel focus: overview, network, storage, console, dom, sources, performance, search, evidence, or debug. Default overview.",
          "enum": [
            "overview",
            "network",
            "storage",
            "console",
            "dom",
            "sources",
            "performance",
            "search",
            "evidence",
            "debug"
          ],
          "type": "string"
        },
        "includeHeavy": {
          "description": "Include heavier evidence passes (source maps, full HAR). Default false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum items per evidence section. Default 20.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Search query forwarded to the search or sources focus routes.",
          "type": "string"
        },
        "requestId": {
          "description": "Network request ID for focused network drill-down.",
          "type": "string"
        },
        "save": {
          "description": "If true, persist the evidence bundle to disk. Default false.",
          "type": "boolean"
        },
        "selector": {
          "description": "CSS selector used when focus is dom to inspect a specific element.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID override. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Create an Agentic Intruder P0.1 planning job from a captured request: validate payload positions, count variants, write profile-local artifacts, and return dry-run preview variants without sending requests.",
    "name": "attack_intruder_create",
    "parameters": {
      "properties": {
        "jobId": {
          "description": "Custom job ID. Defaults to a generated ID.",
          "type": "string"
        },
        "maxPreview": {
          "description": "Max preview variants to include in the dry-run response. Default: 10.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "requestId": {
          "description": "Request ID to use as the base request; can also be specified in spec.source.requestId.",
          "type": "string"
        },
        "spec": {
          "description": "Required. Intruder job specification: positions, payloads, mode, and optional source/profile fields.",
          "type": "object"
        }
      },
      "required": [
        "spec"
      ],
      "type": "object"
    }
  },
  {
    "description": "Write and return an Agentic Intruder P0.1 evidence bundle with spec/state/preview artifact hashes. This is planning evidence only until replay execution lands.",
    "name": "attack_intruder_evidence",
    "parameters": {
      "properties": {
        "jobId": {
          "description": "Attack job ID from attack_intruder_create. Required when jobPath is omitted.",
          "type": "string"
        },
        "jobPath": {
          "description": "Absolute path to the job directory. Takes precedence over jobId.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name to locate the job evidence directory.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Mark an Agentic Intruder job paused in state.json. A running job observes the checkpoint after the current replay batch boundary.",
    "name": "attack_intruder_pause",
    "parameters": {
      "properties": {
        "jobId": {
          "description": "Attack job ID from attack_intruder_create. Required when jobPath is omitted.",
          "type": "string"
        },
        "jobPath": {
          "description": "Absolute path to the job directory. Takes precedence over jobId.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name to locate the job evidence directory.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Read Agentic Intruder result rows and dry-run preview variants. P0.1 jobs return preview data and zero replay results.",
    "name": "attack_intruder_results",
    "parameters": {
      "properties": {
        "jobId": {
          "description": "Attack job ID from attack_intruder_create. Required when jobPath is omitted.",
          "type": "string"
        },
        "jobPath": {
          "description": "Absolute path to the job directory. Takes precedence over jobId.",
          "type": "string"
        },
        "limit": {
          "description": "Max result rows to return. Default: 50.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name to locate the job evidence directory.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Resume an Agentic Intruder job from state.cursor.nextVariantIndex by sending remaining chunks through profile_request_replay_batch.",
    "name": "attack_intruder_resume",
    "parameters": {
      "properties": {
        "batchSize": {
          "description": "Replay batch chunk size. Default: 50.",
          "type": "number"
        },
        "delayMs": {
          "description": "Delay in ms between batches. Default: 0.",
          "type": "number"
        },
        "jobId": {
          "description": "Attack job ID from attack_intruder_create. Required when jobPath is omitted.",
          "type": "string"
        },
        "jobPath": {
          "description": "Absolute path to the job directory. Takes precedence over jobId.",
          "type": "string"
        },
        "maxVariants": {
          "description": "Max variants to send. Default: 200.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name to locate the job evidence directory.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Explicitly run an Agentic Intruder sniper/wordlist job by chunking planned variants and sending each chunk through profile_request_replay_batch. Default maxVariants is 200 and each replay batch is capped at 50.",
    "name": "attack_intruder_run",
    "parameters": {
      "properties": {
        "batchSize": {
          "description": "Replay batch chunk size. Default: 50.",
          "type": "number"
        },
        "delayMs": {
          "description": "Delay in ms between batches. Default: 0.",
          "type": "number"
        },
        "jobId": {
          "description": "Attack job ID from attack_intruder_create. Required when jobPath is omitted.",
          "type": "string"
        },
        "jobPath": {
          "description": "Absolute path to the job directory. Takes precedence over jobId.",
          "type": "string"
        },
        "maxVariants": {
          "description": "Max variants to send. Default: 200.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name to locate the job evidence directory.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Read an Agentic Intruder job state from profile-local attack-job artifacts.",
    "name": "attack_intruder_status",
    "parameters": {
      "properties": {
        "jobId": {
          "description": "Attack job ID from attack_intruder_create. Required when jobPath is omitted.",
          "type": "string"
        },
        "jobPath": {
          "description": "Absolute path to the job directory. Takes precedence over jobId.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name to locate the job evidence directory.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return Accessibility panel-style AX tree for the current profile tab.",
    "name": "browser_accessibility_snapshot",
    "parameters": {
      "properties": {
        "interestingOnly": {
          "description": "If true (default), returns only semantically interesting AX nodes. Set false for the full raw tree.",
          "type": "boolean"
        },
        "maxNodes": {
          "description": "Maximum AX nodes to return. Default 500.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Facade: perform a common browser action: click, hover, double_click, drag, type, press, select, wait, scroll, eval, screenshot, or snapshot.",
    "name": "browser_act",
    "parameters": {
      "properties": {
        "action": {
          "description": "Required. Action to perform.",
          "enum": [
            "click",
            "hover",
            "double_click",
            "dblclick",
            "drag",
            "type",
            "press",
            "select",
            "wait",
            "scroll",
            "eval",
            "screenshot",
            "snapshot"
          ],
          "type": "string"
        },
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "expression": {
          "description": "JS expression for the eval action.",
          "type": "string"
        },
        "observeAfter": {
          "description": "Capture a network observation window after the action.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "returnSnapshot": {
          "description": "Include an accessibility snapshot in the response.",
          "type": "boolean"
        },
        "selector": {
          "description": "CSS selector for click/hover/type/select/drag actions.",
          "type": "string"
        },
        "text": {
          "description": "Text to type for the type action.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitFor": {
          "description": "Selector or condition to wait for.",
          "type": "string"
        },
        "waitMode": {
          "description": "Wait mode for the wait action.",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms. Default: 8000.",
          "type": "number"
        },
        "x": {
          "description": "X coordinate for drag actions.",
          "type": "number"
        },
        "y": {
          "description": "Y coordinate for drag actions.",
          "type": "number"
        }
      },
      "required": [
        "action"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bind an existing live CDP tab to a durable profile. Use this when a visible page exists in the managed browser but the profile registry points at a stale or wrong tab.",
    "name": "browser_adopt_tab",
    "parameters": {
      "properties": {
        "latest": {
          "description": "Adopt the most recently opened tab.",
          "type": "boolean"
        },
        "preferNonBlank": {
          "description": "Skip blank/about:blank tabs when selecting the best match.",
          "type": "boolean"
        },
        "profile": {
          "description": "Required. Profile name to bind.",
          "type": "string"
        },
        "reason": {
          "description": "Human-readable adoption reason recorded in the profile registry.",
          "type": "string"
        },
        "tabId": {
          "description": "Exact tab ID to adopt. Takes precedence over URL/title filters.",
          "type": "string"
        },
        "titleContains": {
          "description": "Adopt the first tab whose title contains this substring.",
          "type": "string"
        },
        "urlContains": {
          "description": "Adopt the first tab whose URL contains this substring.",
          "type": "string"
        }
      },
      "required": [
        "profile"
      ],
      "type": "object"
    }
  },
  {
    "description": "Export Application panel data for the current profile tab to a JSON file: storage, cookies, IndexedDB, CacheStorage, and Service Worker summary.",
    "name": "browser_application_export",
    "parameters": {
      "properties": {
        "includeCacheBodies": {
          "description": "Include response body text for each CacheStorage entry. Default true.",
          "type": "boolean"
        },
        "maxCacheBodyChars": {
          "description": "Maximum characters per cache response body. Default 200000.",
          "type": "number"
        },
        "maxCacheEntries": {
          "description": "Maximum CacheStorage entries to export per cache. Default 500.",
          "type": "number"
        },
        "maxIndexedDbRecords": {
          "description": "Maximum records to export per IndexedDB object store. Default 1000.",
          "type": "number"
        },
        "path": {
          "description": "Optional. Absolute path to write the export JSON. Defaults to a timestamped file in the profile evidence directory.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "List saved profile evidence artifacts by type, size, mtime, and path so agents can choose bounded drill-down targets.",
    "name": "browser_artifact_index",
    "parameters": {
      "properties": {
        "kind": {
          "description": "Filter artifacts by kind/extension (e.g. 'har', 'json', 'png'). Returns all if omitted.",
          "type": "string"
        },
        "maxBytes": {
          "description": "Exclude artifacts larger than this byte count.",
          "type": "number"
        },
        "maxFiles": {
          "description": "Maximum artifact files to enumerate. Default 500.",
          "type": "number"
        },
        "minBytes": {
          "description": "Exclude artifacts smaller than this byte count.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name whose evidence directory is scanned. Defaults to the server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Filter artifact paths by this literal substring. Returns all if omitted.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Inspect a saved local evidence artifact without loading the whole file into context: metadata, bounded preview, JSON/HAR shape, and literal match windows.",
    "name": "browser_artifact_inspect",
    "parameters": {
      "properties": {
        "artifactPath": {
          "description": "Alias for path. Use path preferentially.",
          "type": "string"
        },
        "caseSensitive": {
          "description": "If true, search match is case-sensitive. Default false.",
          "type": "boolean"
        },
        "contextChars": {
          "description": "Characters of surrounding context around each search match. Default 200.",
          "type": "number"
        },
        "maxBytes": {
          "description": "Maximum bytes to read for the preview section. Default 50000.",
          "type": "number"
        },
        "maxMatches": {
          "description": "Maximum search match windows to return when query is provided. Default 20.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path to the evidence artifact file to inspect.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name whose evidence directory is the allowed root. Defaults to server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Optional literal string to search within the artifact; returns bounded match windows.",
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    }
  },
  {
    "description": "Read a bounded slice of a saved local evidence artifact by byte range or line range.",
    "name": "browser_artifact_read",
    "parameters": {
      "properties": {
        "artifactPath": {
          "description": "Alias for path. Use path preferentially.",
          "type": "string"
        },
        "includeBase64": {
          "description": "If true, also return the slice as base64 for binary artifacts. Default false.",
          "type": "boolean"
        },
        "lineCount": {
          "description": "Number of lines to read when using line-range mode. Default 200.",
          "type": "number"
        },
        "maxBytes": {
          "description": "Maximum bytes to read from startByte. Default 200000.",
          "type": "number"
        },
        "path": {
          "description": "Required. Absolute path to the evidence artifact file to read.",
          "type": "string"
        },
        "startByte": {
          "description": "Byte offset to start reading from. Default 0 (file start).",
          "type": "number"
        },
        "startLine": {
          "description": "1-based line number to start reading from (alternative to byte range).",
          "type": "number"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    }
  },
  {
    "description": "Search saved local profile evidence artifacts for a literal query and return bounded match windows plus drill-down paths.",
    "name": "browser_artifact_search",
    "parameters": {
      "properties": {
        "caseSensitive": {
          "description": "If true, match is case-sensitive. Default false.",
          "type": "boolean"
        },
        "contextChars": {
          "description": "Characters of surrounding context around each match. Default 200.",
          "type": "number"
        },
        "kind": {
          "description": "Filter artifact files by kind/extension before searching (e.g. 'har', 'json').",
          "type": "string"
        },
        "maxBytesPerFile": {
          "description": "Maximum bytes to read per file before stopping. Default 1000000.",
          "type": "number"
        },
        "maxFiles": {
          "description": "Maximum artifact files to search. Default 500.",
          "type": "number"
        },
        "maxMatches": {
          "description": "Maximum total matches to return across all files. Default 80.",
          "type": "number"
        },
        "maxMatchesPerFile": {
          "description": "Maximum matches to return per individual file. Default 10.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name whose evidence directory is searched. Defaults to the server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Required. Literal string to search across artifact file contents.",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "description": "Operator-assisted auth bootstrap for managed profiles. Opens or resumes a login page, starts objective capture, and lets a human complete login/2FA/anti-abuse checks in the visible browser. It observes completion; it does not bypass login controls.",
    "name": "browser_auth_bootstrap",
    "parameters": {
      "properties": {
        "action": {
          "description": "Bootstrap phase. start: open login page and begin capture. status: check current auth state. finish: finalize and stop capture. Default: start.",
          "enum": [
            "start",
            "status",
            "finish"
          ],
          "type": "string"
        },
        "label": {
          "description": "Capture label. Default: operator-assisted-auth-bootstrap.",
          "type": "string"
        },
        "loginUrl": {
          "description": "Login page URL to open (start action). Must start with https://.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "stopCaptureOnSuccess": {
          "description": "Stop capture recording when success is detected. Default: true.",
          "type": "boolean"
        },
        "successCookieNames": {
          "description": "Declare success when all these cookie names are present.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "successSelector": {
          "description": "Declare success when this CSS selector matches on the page.",
          "type": "string"
        },
        "successUrlContains": {
          "description": "Declare success when the current URL contains this string.",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after opening the login page. Default: 800.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Facade: collect objective authentication-boundary evidence: cookies, auth headers, tokens, storage, and credentialed requests.",
    "name": "browser_auth_boundary",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "includeTokenScan": {
          "description": "Include token/credential scan results in the report. Default true.",
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum auth-related request entries per section. Default 50.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path to save the report JSON. Auto-generated in evidence/auth/ if omitted.",
          "type": "string"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "save": {
          "description": "If true, persist the report JSON to disk.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID override. Defaults to the profile's current tab.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Collect objective authentication and authorization boundary evidence: cookies, auth headers, token-like values, credentialed requests, storage tokens, and security context.",
    "name": "browser_auth_boundary_report",
    "parameters": {
      "properties": {
        "includeTokenScan": {
          "description": "Include token/credential scan results in the report. Default true.",
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum auth-related request entries per section. Default 50.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path to save the report JSON. Auto-generated in evidence/auth/ if omitted.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "save": {
          "description": "If true, persist the report JSON to disk.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID override. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Explain current backend layer, CDP transport, supported domains, and evidence boundaries.",
    "name": "browser_backend_capabilities",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Product router status: show whether the unified browser facade can use Managed CDP and/or the user's Personal Chrome bridge.",
    "name": "browser_backend_status",
    "parameters": {
      "properties": {
        "includePersonalSnapshot": {
          "description": "Also capture a personal Chrome active tab snapshot. Default: false.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Read one CacheStorage response body by cache name and URL in the current profile tab.",
    "name": "browser_cache_entry_get",
    "parameters": {
      "properties": {
        "cacheName": {
          "description": "Required. Name of the CacheStorage cache (from browser_cache_storage_list).",
          "type": "string"
        },
        "maxBodyChars": {
          "description": "Max characters of response body to return. Default: 200000, clamped to 10000000.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "url": {
          "description": "Required. URL of the cached entry to read.",
          "type": "string"
        }
      },
      "required": [
        "cacheName",
        "url"
      ],
      "type": "object"
    }
  },
  {
    "description": "List CacheStorage caches and request/response metadata for the current profile tab.",
    "name": "browser_cache_storage_list",
    "parameters": {
      "properties": {
        "maxCaches": {
          "description": "Maximum number of caches to list. Default 50.",
          "type": "number"
        },
        "maxEntries": {
          "description": "Maximum entries to return per cache. Default 200.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Product routing map for agents: show Basic, Pentest, and Personal Browser capability lanes, default backend, first tools, and boundaries.",
    "name": "browser_capabilities",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "description": "Agent usability: return the DevTools capability map grouped by F12 panel, first-pass tools, drill-down tools, artifacts, and raw CDP escape hatches.",
    "name": "browser_capability_map",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "description": "Facade: manage F12 recording with start, stop, clear, status, or reload.",
    "name": "browser_capture",
    "parameters": {
      "properties": {
        "action": {
          "description": "Capture action. Default: status.",
          "enum": [
            "start",
            "stop",
            "clear",
            "status",
            "reload",
            "hard_reload"
          ],
          "type": "string"
        },
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "clear": {
          "description": "Clear existing traffic log when starting capture.",
          "type": "boolean"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "label": {
          "description": "Label for the capture session.",
          "type": "string"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMs": {
          "description": "Wait time in ms after reload actions. Default: 8000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Bisect captured managed-browser F12 evidence into Network, page/frame, realtime, and summary buckets.",
    "name": "browser_capture_bisect",
    "parameters": {
      "properties": {
        "limit": {
          "description": "Maximum network requests to include in the bisect output. Default 200.",
          "type": "number"
        },
        "path": {
          "description": "Optional. Absolute path to write the bisect output JSON. Defaults to a timestamped file in the profile evidence directory.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile if omitted.",
          "type": "string"
        },
        "save": {
          "description": "If true (default), saves the bisect output to a file in the evidence directory.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Clear captured F12 events for a managed browser profile.",
    "name": "browser_capture_clear",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Compare two saved evidence artifacts or current captured traffic against a saved artifact. Useful for login/logout, role, account, and permission-boundary before/after research.",
    "name": "browser_capture_diff",
    "parameters": {
      "properties": {
        "afterPath": {
          "description": "Absolute path to the 'after' evidence bundle JSON. Uses current profile traffic if omitted.",
          "type": "string"
        },
        "beforePath": {
          "description": "Required. Absolute path to the 'before' evidence bundle JSON file.",
          "type": "string"
        },
        "includeCurrentAsAfter": {
          "description": "If true and afterPath is omitted, use current profile captured traffic as the 'after' snapshot.",
          "type": "boolean"
        },
        "path": {
          "description": "Absolute path to save the diff JSON. Auto-generated in evidence/diffs/ if omitted.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name used when includeCurrentAsAfter is true. Defaults to the server default profile.",
          "type": "string"
        },
        "save": {
          "description": "If true, persist the diff JSON to disk.",
          "type": "boolean"
        }
      },
      "required": [
        "beforePath"
      ],
      "type": "object"
    }
  },
  {
    "description": "Start explicit F12 capture for a managed browser profile. Appends to the existing traffic log by default — does NOT destroy prior evidence. Pass clear=true only when you explicitly want a fresh segment and accept losing every previously captured request.",
    "name": "browser_capture_start",
    "parameters": {
      "properties": {
        "clear": {
          "description": "Physically delete traffic.jsonl + websockets.jsonl + eventsource.jsonl + in-memory buffer before starting. NOT recoverable. Default false (append mode). Pass true only when starting a clean segment is more valuable than every byte of prior evidence.",
          "type": "boolean"
        },
        "label": {
          "description": "Optional label to tag this capture session (e.g. \"login-flow\", \"checkout\").",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted. Profile must exist (create with profile_create or browser_open).",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Inspect explicit F12 capture status for a managed browser profile.",
    "name": "browser_capture_status",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Stop explicit F12 capture for a managed browser profile.",
    "name": "browser_capture_stop",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Run a raw Chrome DevTools Protocol command against the profile tab for F12 features not yet wrapped as first-class tools.",
    "name": "browser_cdp_command",
    "parameters": {
      "properties": {
        "method": {
          "description": "Required. Chrome DevTools Protocol method, e.g. 'Runtime.evaluate' or 'Network.enable'.",
          "type": "string"
        },
        "params": {
          "description": "CDP method parameters object. Omit or pass {} for methods with no params.",
          "type": "object"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "required": [
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Capture Chrome Tracing data for the current profile tab and write the full trace to evidence.",
    "name": "browser_chrome_trace",
    "parameters": {
      "properties": {
        "categories": {
          "description": "Tracing categories. Default: devtools.timeline, blink.user_timing, loading, netlog, screenshot.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "durationMs": {
          "description": "Capture duration in ms. Default: 1500, range 250–10000.",
          "type": "number"
        },
        "maxEvents": {
          "description": "Max events to include in inline summary. Default: 200.",
          "type": "number"
        },
        "maxScreenshots": {
          "description": "Max screenshot frames to extract from the trace.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "saveScreenshots": {
          "description": "Save screenshot frames to evidence. Default: true.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Click by CSS selector, visible text, or x/y coordinate. For SPA/same-page buttons that should not wait for a navigation, pass waitMode=\"no-navigation\" or waitMode=\"spa\"; pass inputMode=\"dom\" only when a direct HTMLElement.click() is intended. Returns effective: false + evidence block when the click registered with the driver but produced no observable side effect (no navigation, no DOM change, no history change) — usually a sign that the actionability check passed but the SPA's React handler did not fire. Agents seeing effective: false should snapshot the page, reconsider the selector, or fall back to a different inputMode.",
    "name": "browser_click",
    "parameters": {
      "properties": {
        "actionTimeoutMs": {
          "description": "How long to wait for locator/actionability before clicking. Default 3000 ms.",
          "type": "number"
        },
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "frameIndexes": {
          "description": "Optional. Frame index path for nested iframe targeting.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "framePath": {
          "description": "Optional. Frame path string for clicking inside a specific iframe.",
          "type": "string"
        },
        "includeFrames": {
          "description": "Not implemented — driver ignores this flag. To target elements inside an iframe use framePath (iframe CSS selector string, e.g. 'iframe#chat') or frameIndexes (0-based integer array, e.g. [0] for the first iframe).",
          "type": "boolean"
        },
        "includeShadow": {
          "description": "Advisory — not consumed by driver code. Playwright auto-traverses open shadow roots. For explicit shadow-root element targeting use a CSS selector with Playwright's >> deep-piercing syntax.",
          "type": "boolean"
        },
        "inputMode": {
          "description": "Click mechanism. Default \"playwright\" (Playwright locator). Use \"dom\" for a direct HTMLElement.click() fallback on SPA elements.",
          "enum": [
            "playwright",
            "dom"
          ],
          "type": "string"
        },
        "maxShadowRoots": {
          "description": "Advisory — not consumed by driver code. Playwright handles shadow DOM traversal natively with no configurable limit.",
          "type": "number"
        },
        "observeAfter": {
          "description": "Return a bounded after-click page observation.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "returnSnapshot": {
          "description": "Alias for observeAfter.",
          "type": "boolean"
        },
        "selector": {
          "description": "CSS selector for the element to click (e.g. \"#submit\", \"button.primary\"). Use text instead for visible-label matching.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override. Managed: profile tab override. Personal: Chrome tab id.",
          "type": "string"
        },
        "text": {
          "description": "Visible text of the element to click (e.g. \"Sign in\", \"Add to cart\"). Alternative to selector.",
          "type": "string"
        },
        "timeoutMs": {
          "description": "Alias for actionTimeoutMs on the pre-action locator/actionability wait.",
          "type": "number"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMode": {
          "description": "Post-click wait strategy. Default \"settle\". Use \"no-navigation\" or \"spa\" for same-page buttons that do not trigger navigation.",
          "enum": [
            "settle",
            "no-navigation",
            "spa",
            "none"
          ],
          "type": "string"
        },
        "waitMs": {
          "description": "Additional milliseconds to wait after the click action. Default 800.",
          "type": "number"
        },
        "x": {
          "description": "X coordinate for coordinate-based click. Requires y.",
          "type": "number"
        },
        "y": {
          "description": "Y coordinate for coordinate-based click. Requires x.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Capture Console panel events, Log entries, exceptions, stack traces, and parsed script metadata for the current profile tab.",
    "name": "browser_console_log",
    "parameters": {
      "properties": {
        "ignoreCache": {
          "description": "Bypass cache on reload. Default: false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Max events per category to return. Default: 100.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "reload": {
          "description": "Reload the page to capture fresh console output. Default: false.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after page load to collect events. Default: 8000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return source lines around a Console/exception stack frame script location.",
    "name": "browser_console_source_context",
    "parameters": {
      "properties": {
        "columnNumber": {
          "description": "0-based column number from the stack frame.",
          "type": "number"
        },
        "contextLines": {
          "description": "Lines of source context to include before and after the target line. Default: 5.",
          "type": "number"
        },
        "lineNumber": {
          "description": "0-based line number from the stack frame. Default: 0.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "reload": {
          "description": "Reload the page to re-capture scripts. Default: false.",
          "type": "boolean"
        },
        "scriptId": {
          "description": "CDP script ID from a stack frame or browser_sources_list.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "urlContains": {
          "description": "Fallback: find a script whose URL contains this substring when scriptId is not provided.",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after reload. Default: 8000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Summarize browser cookies for the current profile tab, including SameSite, Secure, HttpOnly, expiry, and objective attribute signals.",
    "name": "browser_cookie_summary",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Read cookies from the browser. Use this to lift a logged-in session from personal Chrome and pass it to the managed browser or a VPS scanner. backend is optional — follows the profile's sticky backend, else managed. Schema note (A7): managed returns {ok, backend:\"managed\", count, cookies:[{name,value,domain,path,secure,httpOnly,sameSite,expires}]} at the top level; personal returns the same fields nested under result.result due to the routeToPersonal wrapper — read result.result.cookies when on personal backend.",
    "name": "browser_cookies_get",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Which browser to read cookies from. Optional — follows the profile's sticky backend, else managed.",
          "enum": [
            "managed",
            "personal"
          ],
          "type": "string"
        },
        "domain": {
          "description": "Optional. Filter cookies by domain (e.g. \".bugcrowd.com\" or \"bugcrowd.com\").",
          "type": "string"
        },
        "name": {
          "description": "Optional. Return only the cookie with this exact name.",
          "type": "string"
        },
        "profile": {
          "description": "Managed profile name (defaults to default). Ignored for personal backend.",
          "type": "string"
        },
        "url": {
          "description": "Optional. Filter cookies that apply to this URL.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Inject cookies into the browser. Use this to lift a logged-in session from one backend and apply it to the other, e.g. take a personal Chrome login and resume it in an isolated managed browser to skip OTP. backend is optional — it follows the profile's sticky backend (the mode you picked for this target), else managed; pass backend to override.",
    "name": "browser_cookies_set",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Optional. Follows the profile's sticky backend, else managed. \"managed\" = isolated CDP browser, \"personal\" = the user's real Chrome via the extension bridge.",
          "enum": [
            "managed",
            "personal"
          ],
          "type": "string"
        },
        "cookies": {
          "description": "Array of cookie objects to set.",
          "items": {
            "properties": {
              "domain": {
                "description": "Domain scope for the cookie (e.g. '.example.com'). Provide domain or url.",
                "type": "string"
              },
              "expires": {
                "description": "Expiry in unix seconds.",
                "type": "number"
              },
              "httpOnly": {
                "description": "If true, cookie is inaccessible to JavaScript (HttpOnly flag).",
                "type": "boolean"
              },
              "name": {
                "description": "Cookie name. Required.",
                "type": "string"
              },
              "path": {
                "description": "Path scope for the cookie. Default '/'.",
                "type": "string"
              },
              "sameSite": {
                "description": "SameSite policy: Lax, Strict, or None.",
                "enum": [
                  "Lax",
                  "Strict",
                  "None"
                ],
                "type": "string"
              },
              "secure": {
                "description": "If true, cookie is only sent over HTTPS (Secure flag).",
                "type": "boolean"
              },
              "url": {
                "description": "Playwright accepts url instead of domain+path.",
                "type": "string"
              },
              "value": {
                "description": "Cookie value. Required.",
                "type": "string"
              }
            },
            "required": [
              "name",
              "value"
            ],
            "type": "object"
          },
          "type": "array"
        },
        "profile": {
          "description": "Managed profile name (defaults to default). Ignored for personal backend.",
          "type": "string"
        }
      },
      "required": [
        "cookies"
      ],
      "type": "object"
    }
  },
  {
    "description": "Capture DevTools Coverage-panel drilldown data with raw JavaScript ranges, CSS rule usage, and bounded source snippets.",
    "name": "browser_coverage_detail",
    "parameters": {
      "properties": {
        "durationMs": {
          "description": "Coverage window in ms. Default: 1000, range 250–10000.",
          "type": "number"
        },
        "ignoreCache": {
          "description": "Bypass cache on reload. Default: false.",
          "type": "boolean"
        },
        "includeSource": {
          "description": "Fetch and embed source text snippets. Default: true.",
          "type": "boolean"
        },
        "includeUnused": {
          "description": "Include unused ranges/rules. Default: true.",
          "type": "boolean"
        },
        "includeUsed": {
          "description": "Include used ranges/rules. Default: true.",
          "type": "boolean"
        },
        "maxEntries": {
          "description": "Max script/CSS rule entries to return. Default: 50.",
          "type": "number"
        },
        "maxRangesPerEntry": {
          "description": "Max coverage ranges per script entry. Default: 20.",
          "type": "number"
        },
        "maxSnippetChars": {
          "description": "Max characters per source snippet. Default: 300.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "reload": {
          "description": "Reload page before measuring coverage. Default: false.",
          "type": "boolean"
        },
        "scriptId": {
          "description": "Filter to a specific CDP script ID.",
          "type": "string"
        },
        "styleSheetId": {
          "description": "Filter CSS to a specific style sheet ID.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "urlContains": {
          "description": "Filter scripts by URL substring (case-insensitive).",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Capture short JavaScript precise coverage and CSS rule usage for the current profile tab.",
    "name": "browser_coverage_snapshot",
    "parameters": {
      "properties": {
        "durationMs": {
          "description": "Coverage window in ms. Default: 1000, range 250–10000.",
          "type": "number"
        },
        "maxEntries": {
          "description": "Max script/CSS rule entries in the summary. Default: 200.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Capture a JavaScript CPU profile from the current profile tab and save the full DevTools profile to evidence.",
    "name": "browser_cpu_profile",
    "parameters": {
      "properties": {
        "durationMs": {
          "description": "CPU profiling duration in ms. Default: 1000, range 100–30000.",
          "type": "number"
        },
        "maxNodes": {
          "description": "Max top nodes to include in the summary. Default: 20.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "triggerExpression": {
          "description": "JS expression to evaluate during profiling to trigger the code to measure.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return DevTools Elements-panel Styles/Computed/Box Model evidence for a selected DOM node.",
    "name": "browser_css_styles",
    "parameters": {
      "properties": {
        "forcePseudoClasses": {
          "description": "Pseudo-classes to force during inspection (e.g. [\"hover\", \"focus\"]).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "frameIndexes": {
          "description": "Optional. Frame index path for nested iframe targeting.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "framePath": {
          "description": "Optional. Frame path string for targeting inside a specific iframe.",
          "type": "string"
        },
        "includeBoxModel": {
          "description": "Include box model dimensions. Default true.",
          "type": "boolean"
        },
        "includeComputed": {
          "description": "Include computed styles. Default true.",
          "type": "boolean"
        },
        "includeMatchedRules": {
          "description": "Include matched CSS rules. Default true.",
          "type": "boolean"
        },
        "maxRules": {
          "description": "Maximum CSS rules to return. Default 80.",
          "type": "number"
        },
        "persistPseudoState": {
          "description": "If true, leave forced pseudo-classes active after the call. Default false.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "selector": {
          "description": "CSS selector for the target node. Defaults to body.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Use core DevTools Debugger controls: pause/resume/step, breakpoint setup, XHR breakpoints, and paused call-frame/scope inspection.",
    "name": "browser_debugger_control",
    "parameters": {
      "properties": {
        "action": {
          "description": "Debugger action to perform. Default: snapshot (read paused state without changing it).",
          "enum": [
            "snapshot",
            "pause",
            "resume",
            "stepOver",
            "stepInto",
            "stepOut",
            "pauseOnExpression",
            "setBreakpointByUrl",
            "removeBreakpoint",
            "setXHRBreakpoint",
            "removeXHRBreakpoint",
            "probeBreakpointByUrl"
          ],
          "type": "string"
        },
        "autoResume": {
          "description": "Auto-resume after capturing paused state. Default: true for pause/step actions.",
          "type": "boolean"
        },
        "breakpointId": {
          "description": "Breakpoint ID for removeBreakpoint. Required for that action.",
          "type": "string"
        },
        "columnNumber": {
          "description": "0-based column number for breakpoint actions.",
          "type": "number"
        },
        "condition": {
          "description": "Conditional expression for breakpoint; only pauses when truthy.",
          "type": "string"
        },
        "evaluateExpressions": {
          "description": "Expressions to evaluate in each paused frame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "evaluateReturnByValue": {
          "description": "Return evaluated results by value. Default: true.",
          "type": "boolean"
        },
        "expression": {
          "description": "JS expression for pauseOnExpression. Default: 'debugger;'.",
          "type": "string"
        },
        "ignoreCache": {
          "description": "Bypass cache on reload. Default: false.",
          "type": "boolean"
        },
        "includeCommandLineAPI": {
          "description": "Include Command Line API in evaluation context.",
          "type": "boolean"
        },
        "keepBreakpoint": {
          "description": "Keep the temporary breakpoint after probeBreakpointByUrl. Default: false (auto-remove).",
          "type": "boolean"
        },
        "lineNumber": {
          "description": "0-based line number for breakpoint actions.",
          "type": "number"
        },
        "maxEvaluateExpressions": {
          "description": "Max expressions to evaluate per frame.",
          "type": "number"
        },
        "maxEvaluateFrames": {
          "description": "Max frames to evaluate expressions in.",
          "type": "number"
        },
        "maxEvaluationValueChars": {
          "description": "Max characters for each evaluated value.",
          "type": "number"
        },
        "maxFrames": {
          "description": "Max call frames to include in paused summary.",
          "type": "number"
        },
        "maxProperties": {
          "description": "Max properties per scope object.",
          "type": "number"
        },
        "maxScopes": {
          "description": "Max scope variables per frame.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "reload": {
          "description": "Reload the page to trigger the breakpoint in probeBreakpointByUrl.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "throwOnSideEffect": {
          "description": "Fail evaluation if it has side effects.",
          "type": "boolean"
        },
        "triggerExpression": {
          "description": "JS expression evaluated after probeBreakpointByUrl to trigger the breakpoint.",
          "type": "string"
        },
        "url": {
          "description": "Script URL for setBreakpointByUrl / probeBreakpointByUrl.",
          "type": "string"
        },
        "urlRegex": {
          "description": "Regex pattern for setBreakpointByUrl instead of exact url.",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after the action for breakpoint/pause events. Default: 8000.",
          "type": "number"
        },
        "xhrUrlContains": {
          "description": "URL substring for setXHRBreakpoint / removeXHRBreakpoint.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Facade: compare before/after evidence artifacts or current captured traffic. Requires beforePath and afterPath (absolute paths to bisect JSON artifacts from browser_capture_bisect).",
    "name": "browser_diff",
    "parameters": {
      "properties": {
        "afterPath": {
          "description": "Required. Absolute path to the after-state bisect artifact (from browser_capture_bisect with save=true).",
          "type": "string"
        },
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "beforePath": {
          "description": "Required. Absolute path to the before-state bisect artifact (from browser_capture_bisect with save=true).",
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        }
      },
      "required": [
        "beforePath",
        "afterPath"
      ],
      "type": "object"
    }
  },
  {
    "description": "Watch DOM mutations for a selector, similar to DevTools Elements DOM-breakpoint evidence without pausing JavaScript.",
    "name": "browser_dom_mutation_watch",
    "parameters": {
      "properties": {
        "attributeOldValue": {
          "description": "Include old attribute values in mutation records. Default true.",
          "type": "boolean"
        },
        "attributes": {
          "description": "Observe attribute changes. Default true.",
          "type": "boolean"
        },
        "characterData": {
          "description": "Observe text content changes. Default false.",
          "type": "boolean"
        },
        "characterDataOldValue": {
          "description": "Include old text content values in mutation records. Default false.",
          "type": "boolean"
        },
        "childList": {
          "description": "Observe child node additions/removals. Default true.",
          "type": "boolean"
        },
        "durationMs": {
          "description": "How long to observe mutations in milliseconds. Default 1000, max 10000.",
          "type": "number"
        },
        "maxEvents": {
          "description": "Maximum mutation events to capture. Default 100.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "selector": {
          "description": "Required. CSS selector for the element to watch for mutations.",
          "type": "string"
        },
        "subtree": {
          "description": "Watch all descendants of the target, not just direct children. Default true.",
          "type": "boolean"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "triggerExpression": {
          "description": "Optional JS expression to evaluate in the page context to trigger mutations after observation starts.",
          "type": "string"
        }
      },
      "required": [
        "selector"
      ],
      "type": "object"
    }
  },
  {
    "description": "Search the live DOM using Chrome DevTools DOM.performSearch, like Elements panel search.",
    "name": "browser_dom_search",
    "parameters": {
      "properties": {
        "includeFrames": {
          "description": "Include same-origin iframes in the search. Default true.",
          "type": "boolean"
        },
        "includeUserAgentShadowDOM": {
          "description": "Include user-agent shadow DOM nodes in the search. Default false.",
          "type": "boolean"
        },
        "maxOuterHTMLChars": {
          "description": "Maximum outerHTML characters per result. Default 1200.",
          "type": "number"
        },
        "maxResults": {
          "description": "Maximum results to return. Default 20.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "query": {
          "description": "Required. CSS selector, XPath, or plain text to search in the DOM.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return Chrome DOMSnapshot.captureSnapshot data for the current profile tab.",
    "name": "browser_dom_snapshot",
    "parameters": {
      "properties": {
        "computedStyles": {
          "description": "CSS property names to include in the snapshot. Defaults to a standard set (display, visibility, opacity, position, z-index, color, etc.).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "includeBlendedBackgroundColors": {
          "description": "Include blended background colors. Default false.",
          "type": "boolean"
        },
        "includeDOMRects": {
          "description": "Include bounding box rects for each node. Default true.",
          "type": "boolean"
        },
        "includePaintOrder": {
          "description": "Include paint order for each node. Default true.",
          "type": "boolean"
        },
        "includeTextColorOpacities": {
          "description": "Include text color opacities. Default false.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Double-click an element by CSS selector, visible text, or x/y coordinate. Use for editable table cells, file rows, and controls that require a double-click gesture.",
    "name": "browser_double_click",
    "parameters": {
      "properties": {
        "actionTimeoutMs": {
          "description": "How long to wait for locator/actionability before double-clicking. Default 3000 ms.",
          "type": "number"
        },
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "frameIndexes": {
          "description": "Optional. Frame index path for nested iframe targeting.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "framePath": {
          "description": "Optional. Frame path string for targeting inside a specific iframe.",
          "type": "string"
        },
        "includeFrames": {
          "description": "Not implemented — driver ignores this flag. To target elements inside an iframe use framePath (iframe CSS selector string, e.g. 'iframe#chat') or frameIndexes (0-based integer array, e.g. [0] for the first iframe).",
          "type": "boolean"
        },
        "includeShadow": {
          "description": "Advisory — not consumed by driver code. Playwright auto-traverses open shadow roots. For explicit shadow-root element targeting use a CSS selector with Playwright's >> deep-piercing syntax.",
          "type": "boolean"
        },
        "maxShadowRoots": {
          "description": "Advisory — not consumed by driver code. Playwright handles shadow DOM traversal natively with no configurable limit.",
          "type": "number"
        },
        "observeAfter": {
          "description": "Return a bounded after-double-click page observation.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "returnSnapshot": {
          "description": "Alias for observeAfter.",
          "type": "boolean"
        },
        "selector": {
          "description": "CSS selector for the element to double-click.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "text": {
          "description": "Visible text of the element to double-click. Alternative to selector.",
          "type": "string"
        },
        "timeoutMs": {
          "description": "Alias for actionTimeoutMs on the pre-action locator/actionability wait.",
          "type": "number"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMode": {
          "description": "Post-click wait strategy. Default \"settle\".",
          "enum": [
            "settle",
            "no-navigation",
            "spa",
            "none"
          ],
          "type": "string"
        },
        "waitMs": {
          "description": "Additional milliseconds to wait after the double-click. Default 800.",
          "type": "number"
        },
        "x": {
          "description": "X coordinate for coordinate-based double-click. Requires y.",
          "type": "number"
        },
        "y": {
          "description": "Y coordinate for coordinate-based double-click. Requires x.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Managed CDP download watcher. Start before triggering a download, then query status for Browser.downloadWillBegin/downloadProgress events.",
    "name": "browser_download_watch",
    "parameters": {
      "properties": {
        "action": {
          "description": "Required action: 'start' to begin watching, 'status' to query progress, 'stop' to stop.",
          "type": "string"
        },
        "dir": {
          "description": "Directory to monitor for downloaded files. Used with start action.",
          "type": "string"
        },
        "downloadPath": {
          "description": "Explicit file path to watch for completion. Used with start action.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name used to scope the download watcher. Defaults to the server default profile.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Drag one element to another element or point. Use selector (CSS selector) or text (visible text) to identify the drag source; use targetSelector/toSelector or targetText/toText for the drop target; or use deltaX/deltaY to drag by a pixel offset. Do NOT use 'from'/'to' parameter names — use selector/targetSelector.",
    "name": "browser_drag",
    "parameters": {
      "properties": {
        "actionTimeoutMs": {
          "description": "How long to wait for source/target locator actionability. Default 3000 ms.",
          "type": "number"
        },
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "deltaX": {
          "description": "Pixel offset in X from the source element center.",
          "type": "number"
        },
        "deltaY": {
          "description": "Pixel offset in Y from the source element center.",
          "type": "number"
        },
        "frameIndexes": {
          "description": "Optional. Frame index path for nested iframe source.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "framePath": {
          "description": "Optional. Frame path for source element inside a specific iframe.",
          "type": "string"
        },
        "includeFrames": {
          "description": "Not implemented — driver ignores this flag. To target elements inside an iframe use framePath (iframe CSS selector string, e.g. 'iframe#chat') or frameIndexes (0-based integer array, e.g. [0] for the first iframe).",
          "type": "boolean"
        },
        "includeShadow": {
          "description": "Advisory — not consumed by driver code. Playwright auto-traverses open shadow roots. For explicit shadow-root element targeting use a CSS selector with Playwright's >> deep-piercing syntax.",
          "type": "boolean"
        },
        "maxShadowRoots": {
          "description": "Advisory — not consumed by driver code. Playwright handles shadow DOM traversal natively with no configurable limit.",
          "type": "number"
        },
        "observeAfter": {
          "description": "Return a bounded after-drag page observation.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "returnSnapshot": {
          "description": "Alias for observeAfter.",
          "type": "boolean"
        },
        "selector": {
          "description": "CSS selector for the drag source element.",
          "type": "string"
        },
        "steps": {
          "description": "Number of intermediate steps for the drag gesture. Higher = smoother.",
          "type": "number"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "targetFrameIndexes": {
          "description": "Optional. Frame index path for nested iframe drop target.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "targetFramePath": {
          "description": "Optional. Frame path for the drop target element.",
          "type": "string"
        },
        "targetSelector": {
          "description": "CSS selector for the drop target element.",
          "type": "string"
        },
        "targetText": {
          "description": "Visible text of the drop target. Alternative to targetSelector.",
          "type": "string"
        },
        "text": {
          "description": "Visible text of the drag source element. Alternative to selector.",
          "type": "string"
        },
        "timeoutMs": {
          "description": "Alias for actionTimeoutMs.",
          "type": "number"
        },
        "toSelector": {
          "description": "Alias for targetSelector.",
          "type": "string"
        },
        "toText": {
          "description": "Alias for targetText.",
          "type": "string"
        },
        "toX": {
          "description": "Destination X coordinate for coordinate-based drag.",
          "type": "number"
        },
        "toY": {
          "description": "Destination Y coordinate for coordinate-based drag.",
          "type": "number"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMs": {
          "description": "Additional milliseconds to wait after drag. Default 500.",
          "type": "number"
        },
        "x": {
          "description": "Source X coordinate for coordinate-based drag.",
          "type": "number"
        },
        "y": {
          "description": "Source Y coordinate for coordinate-based drag.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return DOM tree, layout boxes, and computed style for the current profile tab.",
    "name": "browser_elements_snapshot",
    "parameters": {
      "properties": {
        "maxDepth": {
          "description": "Maximum tree depth to traverse. Default 6.",
          "type": "number"
        },
        "maxNodes": {
          "description": "Maximum DOM nodes to include in the tree. Default 250.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "selector": {
          "description": "Optional CSS selector to inspect a specific element in detail alongside the tree.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Evaluate a JavaScript expression in the current tab and return its value. Required param: expression (not 'code' or 'script'). Supported on both managed and personal backends: managed uses CDP Runtime.evaluate (bypasses page CSP); personal uses chrome.scripting.executeScript with a custom return wrapper. Returns {ok, result, exception}. 'Local trusted use only' means this tool is for authorised agent use on test targets — not for untrusted third-party page content.",
    "name": "browser_eval",
    "parameters": {
      "properties": {
        "expression": {
          "description": "Required. JavaScript expression to evaluate in the page context. Use expression= not code= or script=.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "required": [
        "expression"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return DevTools Elements-panel Event Listeners for a selected DOM node.",
    "name": "browser_event_listeners",
    "parameters": {
      "properties": {
        "depth": {
          "description": "Depth of the ancestor chain to include event listeners for. Default -1 (full chain).",
          "type": "number"
        },
        "frameIndexes": {
          "description": "Optional. Frame index path for nested iframe targeting.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "framePath": {
          "description": "Optional. Frame path string for targeting inside a specific iframe.",
          "type": "string"
        },
        "pierce": {
          "description": "If true, pierce shadow roots to find listeners. Default true.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "selector": {
          "description": "CSS selector for the DOM node. Defaults to document if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Export a compact objective F12 evidence bundle for the current profile: diagnostics, Network summary, Issues, Security, Storage summary, and Sources list.",
    "name": "browser_evidence_bundle",
    "parameters": {
      "properties": {
        "fullPageScreenshot": {
          "description": "If true, capture full-page screenshot instead of viewport. Default false.",
          "type": "boolean"
        },
        "includeHar": {
          "description": "If true, include a full HAR export section. Default false.",
          "type": "boolean"
        },
        "includeScreenshot": {
          "description": "Include a screenshot section. Default true.",
          "type": "boolean"
        },
        "includeSnapshot": {
          "description": "Include a DOM snapshot section. Default true.",
          "type": "boolean"
        },
        "includeTokenFlow": {
          "description": "Include a token flow trace section (JavaScript token propagation). Default false.",
          "type": "boolean"
        },
        "includeTokenScan": {
          "description": "Include a token scan section for credential/token pattern detection. Default false.",
          "type": "boolean"
        },
        "networkLimit": {
          "description": "Maximum network records in the network summary and HAR. Default 10.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path to save the bundle JSON file. Auto-generated if omitted.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "save": {
          "description": "If true (default), persist the bundle to disk and return the file path.",
          "type": "boolean"
        },
        "sourceLimit": {
          "description": "Maximum source scripts to include in the sources list section. Default 100.",
          "type": "number"
        },
        "tabId": {
          "description": "CDP tab ID override. Defaults to the profile's current tab.",
          "type": "string"
        },
        "tokenFlowTriggerExpression": {
          "description": "JavaScript expression to trigger before token flow trace. Used only when includeTokenFlow is true.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Create a manifest for profile evidence files: capture window, artifact paths, sizes, hashes, and local provenance. This is objective evidence bookkeeping, not vulnerability analysis.",
    "name": "browser_evidence_manifest",
    "parameters": {
      "properties": {
        "artifactPaths": {
          "description": "Explicit list of artifact file paths to include alongside auto-discovered evidence directory files.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxFiles": {
          "description": "Maximum evidence directory files to enumerate. Default 200.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path to save the manifest JSON file. Auto-generated in evidence/manifests/ if omitted.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "save": {
          "description": "If true (default), persist the manifest JSON to disk.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID override. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Build an objective chronological timeline across captured Network, Console, Issues, realtime channels, and saved evidence artifacts.",
    "name": "browser_evidence_timeline",
    "parameters": {
      "properties": {
        "eventType": {
          "description": "Filter by event type (e.g. 'network', 'console', 'issue', 'websocket').",
          "type": "string"
        },
        "includeArtifacts": {
          "description": "Include saved evidence artifact file entries. Default true.",
          "type": "boolean"
        },
        "includeConsole": {
          "description": "Include console log events. Default true.",
          "type": "boolean"
        },
        "includeIssues": {
          "description": "Include Chrome Issues log events. Default true.",
          "type": "boolean"
        },
        "includeRealtime": {
          "description": "Include WebSocket and EventSource realtime events. Default true.",
          "type": "boolean"
        },
        "maxArtifacts": {
          "description": "Maximum evidence artifact file entries to include. Default 200.",
          "type": "number"
        },
        "maxEvents": {
          "description": "Maximum total timeline events to include. Default 500.",
          "type": "number"
        },
        "maxNetworkRecords": {
          "description": "Maximum network records to include. Default 500.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Filter timeline events by literal string match in event data.",
          "type": "string"
        },
        "since": {
          "description": "ISO 8601 timestamp. Only include events at or after this time.",
          "type": "string"
        },
        "source": {
          "description": "Filter timeline by event source label.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID override. Defaults to the profile's current tab.",
          "type": "string"
        },
        "until": {
          "description": "ISO 8601 timestamp. Only include events at or before this time.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Reload the Personal Chrome extension when that backend is active. No-op for managed CDP.",
    "name": "browser_extension_reload",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "description": "Agent usability: return an objective F12 parity matrix for professional AppSec work, including supported panels, partial coverage, tool routes, and browser boundaries.",
    "name": "browser_f12_parity_matrix",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "description": "Agent feedback entrypoint: record a local bug, capability gap, docs issue, product friction, or idea about Agent Browser Runtime. This writes a local feedback/*.md note and does not judge vulnerabilities.",
    "name": "browser_feedback",
    "parameters": {
      "properties": {
        "actual": {
          "description": "What actually happened.",
          "type": "string"
        },
        "evidence": {
          "description": "Relevant evidence (tool output, error message, file path).",
          "type": "string"
        },
        "expected": {
          "description": "What the agent expected to happen.",
          "type": "string"
        },
        "next": {
          "description": "Suggested next action or fix direction.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name associated with the feedback, if applicable.",
          "type": "string"
        },
        "reporter": {
          "description": "Agent or system that generated the feedback. Defaults to browser-worker-tool.",
          "type": "string"
        },
        "repro": {
          "description": "Reproduction steps for bugs.",
          "type": "string"
        },
        "summary": {
          "description": "Detailed description of the issue or idea.",
          "type": "string"
        },
        "title": {
          "description": "Required. Short one-line title for the feedback note.",
          "type": "string"
        },
        "tool": {
          "description": "The tool name this feedback is about (e.g. browser_click).",
          "type": "string"
        },
        "type": {
          "description": "Feedback category.",
          "enum": [
            "bug",
            "gap",
            "docs",
            "product",
            "idea"
          ],
          "type": "string"
        }
      },
      "required": [
        "title"
      ],
      "type": "object"
    }
  },
  {
    "description": "Find interactive elements on the page using a natural-language query (e.g. \"search bar\", \"add to cart button\", \"login link\"). Matches against innerText, aria-label, placeholder, title, name, role and tag, returns ranked candidates with selectors and bounding boxes so the agent can click/type without writing a CSS selector by hand. Backend note (A5): browser_find is managed-backend only (Playwright DOM search); it does not have a personal-backend route and will silently run on the managed browser even when a profile's sticky backend is personal. For personal-backend DOM search use browser_dom_search instead.",
    "name": "browser_find",
    "parameters": {
      "properties": {
        "includeFrames": {
          "description": "Search same-origin iframes. Default true.",
          "type": "boolean"
        },
        "includeNonInteractive": {
          "description": "Include any element with matching text, not just clickable controls.",
          "type": "boolean"
        },
        "includeShadow": {
          "description": "Search open shadow roots. Default true.",
          "type": "boolean"
        },
        "maxResults": {
          "description": "Default 10, max 30.",
          "type": "number"
        },
        "maxShadowRoots": {
          "description": "Maximum open shadow roots to inspect. Default 60.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Natural-language description of the target element (e.g. \"sign in button\", \"email input\", \"product title containing organic\").",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID override. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return the current Page frame tree for the profile tab.",
    "name": "browser_frame_tree",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Search F12 evidence surfaces for a literal query across Network records, parsed Sources, Storage, IndexedDB samples, and Cache metadata.",
    "name": "browser_global_search",
    "parameters": {
      "properties": {
        "caseSensitive": {
          "description": "If true, match is case-sensitive. Default false.",
          "type": "boolean"
        },
        "contextChars": {
          "description": "Characters of surrounding context to include around each match. Default 120.",
          "type": "number"
        },
        "ignoreCache": {
          "description": "Bypass Chrome's response cache when reloading sources. Default false.",
          "type": "boolean"
        },
        "includeCacheBodies": {
          "description": "If true, fetch and search CacheStorage response bodies. Default false.",
          "type": "boolean"
        },
        "includeNetwork": {
          "description": "Search captured Network records (URL, headers, postData). Default true.",
          "type": "boolean"
        },
        "includeSources": {
          "description": "Search parsed JavaScript source scripts. Default true.",
          "type": "boolean"
        },
        "includeStorage": {
          "description": "Search localStorage, sessionStorage, cookies, and IndexedDB samples. Default true.",
          "type": "boolean"
        },
        "maxCacheBodyChars": {
          "description": "Maximum characters to read from each cache response body. Default 50000.",
          "type": "number"
        },
        "maxCacheEntries": {
          "description": "Maximum CacheStorage entries to scan. Default 50.",
          "type": "number"
        },
        "maxIndexedDbRecords": {
          "description": "Maximum IndexedDB sample records to scan. Default 20.",
          "type": "number"
        },
        "maxMatches": {
          "description": "Maximum total matches to return across all surfaces. Default 80.",
          "type": "number"
        },
        "maxNetworkRecords": {
          "description": "Maximum network records to scan. Default 1000.",
          "type": "number"
        },
        "maxScripts": {
          "description": "Maximum number of source scripts to search. Default 200.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Required. Literal string to search across all enabled evidence surfaces.",
          "type": "string"
        },
        "reloadSources": {
          "description": "Re-fetch source scripts from CDP before searching. Default false.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "waitMs": {
          "description": "Milliseconds to wait for async evidence collection before searching. Default 0.",
          "type": "number"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "description": "Disable cache, optionally bypass service worker, clear profile-local traffic, and reload the tab.",
    "name": "browser_hard_reload",
    "parameters": {
      "properties": {
        "bypassServiceWorker": {
          "description": "Bypass service worker on reload. Default: true.",
          "type": "boolean"
        },
        "clearLog": {
          "description": "Clear captured traffic before reload. Default: true.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after reload for traffic capture. Default: 8000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Capture a DevTools Memory panel JavaScript heap snapshot through HeapProfiler and save the full .heapsnapshot evidence file.",
    "name": "browser_heap_snapshot",
    "parameters": {
      "properties": {
        "exposeInternals": {
          "description": "Expose V8 internals in the snapshot. Default: false.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "reportProgress": {
          "description": "Emit HeapProfiler progress events during capture. Default: true.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Hover over an element by CSS selector, visible text, or x/y coordinate. Use for menus, tooltips, and controls revealed on mouseover.",
    "name": "browser_hover",
    "parameters": {
      "properties": {
        "actionTimeoutMs": {
          "description": "How long to wait for locator/actionability before hovering. Default 3000 ms.",
          "type": "number"
        },
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "frameIndexes": {
          "description": "Optional. Frame index path for nested iframe targeting.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "framePath": {
          "description": "Optional. Frame path string for hovering inside a specific iframe.",
          "type": "string"
        },
        "includeFrames": {
          "description": "Not implemented — driver ignores this flag. To target elements inside an iframe use framePath (iframe CSS selector string, e.g. 'iframe#chat') or frameIndexes (0-based integer array, e.g. [0] for the first iframe).",
          "type": "boolean"
        },
        "includeShadow": {
          "description": "Advisory — not consumed by driver code. Playwright auto-traverses open shadow roots. For explicit shadow-root element targeting use a CSS selector with Playwright's >> deep-piercing syntax.",
          "type": "boolean"
        },
        "maxShadowRoots": {
          "description": "Advisory — not consumed by driver code. Playwright handles shadow DOM traversal natively with no configurable limit.",
          "type": "number"
        },
        "observeAfter": {
          "description": "Return a bounded after-hover page observation.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "returnSnapshot": {
          "description": "Alias for observeAfter.",
          "type": "boolean"
        },
        "selector": {
          "description": "CSS selector for the element to hover over.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "text": {
          "description": "Visible text of the element to hover over. Alternative to selector.",
          "type": "string"
        },
        "timeoutMs": {
          "description": "Alias for actionTimeoutMs on the pre-action locator/actionability wait.",
          "type": "number"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMs": {
          "description": "Additional milliseconds to wait after hover. Default 300.",
          "type": "number"
        },
        "x": {
          "description": "X coordinate for coordinate-based hover. Requires y.",
          "type": "number"
        },
        "y": {
          "description": "Y coordinate for coordinate-based hover. Requires x.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "List IndexedDB databases, object stores, indexes, and record counts for the current profile tab.",
    "name": "browser_indexeddb_list",
    "parameters": {
      "properties": {
        "includeCounts": {
          "description": "Include record counts per object store. Default true.",
          "type": "boolean"
        },
        "maxDatabases": {
          "description": "Maximum IndexedDB databases to list. Default 50.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Read records from a specific IndexedDB database and object store in the current profile tab.",
    "name": "browser_indexeddb_read",
    "parameters": {
      "properties": {
        "database": {
          "description": "Required. Name of the IndexedDB database.",
          "type": "string"
        },
        "limit": {
          "description": "Maximum records to return. Default 50.",
          "type": "number"
        },
        "offset": {
          "description": "Number of records to skip (for pagination). Default 0.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "store": {
          "description": "Required. Name of the object store to read records from.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "required": [
        "database",
        "store"
      ],
      "type": "object"
    }
  },
  {
    "description": "Facade: inspect the current page through agent_inspect. Modes: overview, network, storage, console, dom, sources, performance, search, evidence, debug.",
    "name": "browser_inspect",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "includeHeavy": {
          "description": "Include heavy data (full DOM, all scripts). Default: false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Max items to return per category.",
          "type": "number"
        },
        "mode": {
          "description": "Inspection mode. Default: overview.",
          "enum": [
            "overview",
            "network",
            "storage",
            "console",
            "dom",
            "sources",
            "performance",
            "search",
            "evidence",
            "debug"
          ],
          "type": "string"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Search query for search mode.",
          "type": "string"
        },
        "requestId": {
          "description": "Request ID for network drilldown.",
          "type": "string"
        },
        "selector": {
          "description": "CSS selector for dom mode.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return Chrome DevTools Issues-panel events reported by the browser for the current page.",
    "name": "browser_issues_log",
    "parameters": {
      "properties": {
        "ignoreCache": {
          "description": "If true, reload with cache bypassed. Default false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum issue events to return. Default 100.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "reload": {
          "description": "If true (default), reload the page to trigger fresh issue events before collecting.",
          "type": "boolean"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "waitMs": {
          "description": "Milliseconds to wait after reload for issues to be emitted. Default 1200, clamped to 30000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return DevTools Memory/Performance Monitor-style counters: JS heap usage, DOM counters, and performance metrics.",
    "name": "browser_memory_snapshot",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Navigate an existing open tab to a new URL. Low-level: does not create or register a profile, does not set the sticky backend. Use browser_open instead for ordinary agent work — browser_open creates/resumes the profile record, sets sticky backend, and returns diagnostics. Use browser_navigate only when you already have a live tab bound to a profile and just need to change its URL without profile lifecycle overhead. Both managed and personal backends return top-level `url` and `tabId` fields.",
    "name": "browser_navigate",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Chrome tab id (personal backend) or profile tab id override (managed backend).",
          "type": "string"
        },
        "url": {
          "description": "Required. URL to navigate to (must start with http://, https://, data:, or file:).",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMs": {
          "description": "Milliseconds to wait after navigation for the page to settle. Default 800.",
          "type": "number"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return an agent-friendly list of visible controls with stable selector candidates and suggested CLI actions.",
    "name": "browser_observe",
    "parameters": {
      "properties": {
        "includeHidden": {
          "description": "If true, include hidden/invisible controls. Default false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum controls to return. Default 60, max 200.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Facade: open a URL in a managed or personal browser profile, register the profile record, and return page diagnostics. Prefer this over browser_navigate for all ordinary agent work — it handles profile lifecycle (create/resume), records sticky backend (pass --backend once; all subsequent calls on this profile follow automatically without re-specifying backend), and returns diagnostics + capturedTraffic. Omitting url when the profile has no live page returns error browser_open_requires_url_or_live_profile. First-time use: pass backend=managed (default, Playwright-driven Chrome isolation) or backend=personal (user's real Chrome via extension bridge, for anti-bot bypass only).",
    "name": "browser_open",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "url": {
          "description": "URL to open. Required for a new profile with no live page.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMs": {
          "description": "Wait time in ms after navigation. Default: 8000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return a dashboard-friendly page health summary across Network, Security, Storage, Console, and Accessibility.",
    "name": "browser_page_diagnostics",
    "parameters": {
      "properties": {
        "limit": {
          "description": "Max items per network category in the summary. Default: 5.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Summarize Performance panel timing, slow resources, long tasks, and optional Chrome trace evidence for agents.",
    "name": "browser_performance_insights",
    "parameters": {
      "properties": {
        "durationMs": {
          "description": "Observation window in ms. Default: 500.",
          "type": "number"
        },
        "includeChromeTrace": {
          "description": "Also capture a Chrome trace and include it in insights. Default: false.",
          "type": "boolean"
        },
        "maxEvents": {
          "description": "Max Chrome trace events when includeChromeTrace=true. Default: 20.",
          "type": "number"
        },
        "maxItems": {
          "description": "Max items per insights category. Default: 10.",
          "type": "number"
        },
        "maxScreenshots": {
          "description": "Max screenshot frames from Chrome trace.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "saveScreenshots": {
          "description": "Save screenshot frames to evidence.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Capture browser PerformanceObserver entries such as LCP, layout shift, long tasks, event timing, and long animation frames.",
    "name": "browser_performance_observer",
    "parameters": {
      "properties": {
        "durationMs": {
          "description": "Observation window in ms. Default: 1000, range 100–15000.",
          "type": "number"
        },
        "durationThreshold": {
          "description": "Min duration ms for event entries. Default: 16.",
          "type": "number"
        },
        "entryTypes": {
          "description": "PerformanceObserver entry types to observe. Default: navigation, resource, paint, largest-contentful-paint, layout-shift, longtask, event, long-animation-frame.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxEntries": {
          "description": "Max entries to capture. Default: 500.",
          "type": "number"
        },
        "maxItems": {
          "description": "Max items per category in the summary. Default: 10.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "triggerExpression": {
          "description": "JS expression to evaluate at observation start to trigger timed events.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Capture a short Performance panel-style snapshot for the current profile tab.",
    "name": "browser_performance_trace",
    "parameters": {
      "properties": {
        "durationMs": {
          "description": "Observation window in ms. Default: 3000, max 15000.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Press a keyboard key or shortcut in the page, optionally after focusing a selector.",
    "name": "browser_press",
    "parameters": {
      "properties": {
        "actionTimeoutMs": {
          "description": "How long to wait for the optional selector to become focusable. Default 3000 ms, max 60000.",
          "type": "number"
        },
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "frameIndexes": {
          "description": "Optional. Frame index path for nested iframe targeting.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "framePath": {
          "description": "Optional. Frame path for key press inside a specific iframe.",
          "type": "string"
        },
        "includeFrames": {
          "description": "Not implemented — driver ignores this flag. To target elements inside an iframe use framePath (iframe CSS selector string, e.g. 'iframe#chat') or frameIndexes (0-based integer array, e.g. [0] for the first iframe).",
          "type": "boolean"
        },
        "includeShadow": {
          "description": "Advisory — not consumed by driver code. Playwright auto-traverses open shadow roots. For explicit shadow-root element targeting use a CSS selector with Playwright's >> deep-piercing syntax.",
          "type": "boolean"
        },
        "key": {
          "description": "Required. Key or combo such as Enter, Escape, Tab, Ctrl+K, Control+Enter.",
          "type": "string"
        },
        "maxShadowRoots": {
          "description": "Advisory — not consumed by driver code. Playwright handles shadow DOM traversal natively with no configurable limit.",
          "type": "number"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "selector": {
          "description": "Optional CSS selector to focus before pressing the key.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "timeoutMs": {
          "description": "Alias for actionTimeoutMs on the optional focus wait.",
          "type": "number"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        }
      },
      "required": [
        "key"
      ],
      "type": "object"
    }
  },
  {
    "description": "Managed CDP only: run a raw Chrome DevTools Protocol command against the browser-process endpoint for Browser/SystemInfo/Target-level features.",
    "name": "browser_process_cdp",
    "parameters": {
      "properties": {
        "method": {
          "description": "Required. Chrome DevTools Protocol browser-process method, e.g. 'Browser.getVersion' or 'Target.getTargets'.",
          "type": "string"
        },
        "params": {
          "description": "CDP method parameters object. Omit or pass {} for methods with no params.",
          "type": "object"
        }
      },
      "required": [
        "method"
      ],
      "type": "object"
    }
  },
  {
    "description": "Managed CDP: list browser targets from Target.getTargets for agent target/session discovery.",
    "name": "browser_process_targets",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "description": "Managed CDP: return browser-process version metadata from Browser.getVersion.",
    "name": "browser_process_version",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "description": "Agent usability: report whether the professional F12 evidence workflow is mechanically ready, which evidence pieces are present, and which objective tool to call next.",
    "name": "browser_professional_readiness",
    "parameters": {
      "properties": {
        "includeArtifacts": {
          "description": "Include artifact index summary. Default true.",
          "type": "boolean"
        },
        "includeCaptureBisect": {
          "description": "Include capture bisect analysis to diagnose missing evidence. Default true.",
          "type": "boolean"
        },
        "includeHarCompleteness": {
          "description": "Include HAR completeness check for network body coverage. Default true.",
          "type": "boolean"
        },
        "includeTimeline": {
          "description": "Include evidence timeline summary. Default true.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name to check readiness for. Defaults to the server default profile.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Inspect managed browser profile status: profile name, active tab, evidence directory, CDP endpoint, and capture state.",
    "name": "browser_profile_status",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Managed CDP: discover Chrome DevTools Protocol domains, commands, events, and parameters exposed by the current browser.",
    "name": "browser_protocol_schema",
    "parameters": {
      "properties": {
        "domain": {
          "description": "Filter results to a specific CDP domain name (e.g. 'Network', 'Runtime').",
          "type": "string"
        },
        "includeDeprecated": {
          "description": "Include deprecated CDP methods and events. Default true.",
          "type": "boolean"
        },
        "includeExperimental": {
          "description": "Include experimental CDP methods and events. Default true.",
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum methods/events rows to return. Default 50.",
          "type": "number"
        },
        "query": {
          "description": "Literal substring filter applied to method/event names and descriptions.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Facade: advanced escape hatch. Call one exact devtools_* / browser_* / profile_* tool when the big tools are not enough.",
    "name": "browser_raw",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "input": {
          "description": "Parameter object for the target tool.",
          "type": "object"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "tool": {
          "description": "Required. Tool name to call: devtools_*, browser_*, or profile_*.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        }
      },
      "required": [
        "tool"
      ],
      "type": "object"
    }
  },
  {
    "description": "Scenario readiness check. Use before Basic, Pentest, or Personal Browser work to get explicit blockers and next tools.",
    "name": "browser_ready",
    "parameters": {
      "properties": {
        "checkStuck": {
          "description": "Check for stuck/blank/CAPTCHA page signals. Default: true.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name to check. Defaults to server default profile.",
          "type": "string"
        },
        "scenario": {
          "description": "Readiness scenario to check. Values: basic (page ops), pentest (F12/security), personal (user's Chrome). Default: basic.",
          "enum": [
            "basic",
            "pentest",
            "personal"
          ],
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Facade: replay one captured request or run batch variants and compare responses.",
    "name": "browser_replay",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "requestId": {
          "description": "Required. Request ID from profile_traffic_query to replay.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "variants": {
          "description": "Variant override objects for batch replay. If provided uses profile_request_replay_batch.",
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "requestId"
      ],
      "type": "object"
    }
  },
  {
    "description": "Build an objective graph connecting frames, scripts, Network requests, and Console entries observed in current F12 evidence.",
    "name": "browser_request_correlation_graph",
    "parameters": {
      "properties": {
        "limit": {
          "description": "Maximum network records to include in the graph. Default 200.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path to save the graph JSON. Auto-generated if omitted.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "save": {
          "description": "If true, persist the graph JSON to disk.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID override. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Facade alias for profile_resume. Use this as the first recovery step when a role/profile looks disconnected after an agent session was closed.",
    "name": "browser_resume_profile",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Required. Profile name to resume.",
          "type": "string"
        },
        "reload": {
          "description": "Force reload even when the tab is already attached. Default: false.",
          "type": "boolean"
        },
        "url": {
          "description": "URL to open if the profile has no live tab. Defaults to the profile's last URL.",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after navigation. Default: 800.",
          "type": "number"
        }
      },
      "required": [
        "profile"
      ],
      "type": "object"
    }
  },
  {
    "description": "Run horizontal authorization probe (BOLA) against endpoints identified in the scan corpus. Reads corpus from targets/active/<target>/evidence/, swaps account IDs, sends GET probes, writes bola-results.json. Default is dry run — pass execute:true to send real HTTP probes. Run browser_scan_bridge first to build the corpus.",
    "name": "browser_scan_bola",
    "parameters": {
      "properties": {
        "execute": {
          "description": "If false (default), dry run only — no real HTTP probes sent. Set to true to run live probes.",
          "type": "boolean"
        },
        "maxEndpoints": {
          "description": "Maximum endpoints to probe. Default 30.",
          "type": "number"
        },
        "sleep": {
          "description": "Seconds to sleep between probes for rate-limiting. Default 0.2.",
          "type": "number"
        },
        "target": {
          "description": "Target name (e.g. 'mapbox'). Must match a directory in targets/active/<target>/evidence/ containing responses-captured.jsonl.",
          "type": "string"
        },
        "timeoutMs": {
          "description": "Subprocess timeout in milliseconds. Default 120000.",
          "type": "number"
        },
        "victimIds": {
          "description": "Optional comma-separated victim account IDs to probe with (e.g. 'id1,id2'). If omitted, uses IDs discovered from corpus.",
          "type": "string"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "description": "Bridge ABR-captured browser traffic for a profile into a response corpus for scan analysis. Reads the profile's already-captured network traffic, filters to API-like authenticated endpoints within the given scope domains, redacts credentials, and writes responses-captured.jsonl. Run this before browser_scan_bola.",
    "name": "browser_scan_bridge",
    "parameters": {
      "properties": {
        "profile": {
          "description": "ABR profile name whose captured traffic to read (e.g. 'mapbox-auth').",
          "type": "string"
        },
        "scope": {
          "description": "Scope domain(s) to include (e.g. ['api.mapbox.com', 'account.mapbox.com']). Only requests to these domains are included.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "target": {
          "description": "Target name for output path (e.g. 'mapbox'). Output written to targets/active/<target>/evidence/responses-captured.jsonl.",
          "type": "string"
        }
      },
      "required": [
        "profile",
        "target",
        "scope"
      ],
      "type": "object"
    }
  },
  {
    "description": "Check what scan output files are available for a target. Returns a list of files found in targets/active/<target>/evidence/ that are produced by the scan pipeline (responses-captured.jsonl, bola-results.json, etc.).",
    "name": "browser_scan_status",
    "parameters": {
      "properties": {
        "target": {
          "description": "Target name (e.g. 'mapbox').",
          "type": "string"
        }
      },
      "required": [
        "target"
      ],
      "type": "object"
    }
  },
  {
    "description": "Capture a PNG screenshot from the current browser tab. Returns the base64 image as MCP image content so the agent can see it, plus writes a PNG file under the profile evidence directory. Pass includeImage: false to skip the image payload (e.g. for very large captures). Both managed and personal backends return top-level `path`, `mimeType`, and `bytes` fields; on personal backend `bytes` is the base64 character count (not raw PNG size).",
    "name": "browser_screenshot",
    "parameters": {
      "properties": {
        "fullPage": {
          "description": "If true, captures the full scrollable page. Default false (viewport only).",
          "type": "boolean"
        },
        "includeImage": {
          "description": "When true (default), returns the PNG as base64 image content visible to the agent. When false, only writes to disk and returns metadata.",
          "type": "boolean"
        },
        "maxImageBytes": {
          "description": "If set, only inline the image when the PNG is smaller than this many bytes. Default 4_000_000 (~4 MB).",
          "type": "number"
        },
        "path": {
          "description": "Optional. Absolute path to save the PNG file. Defaults to a timestamped file in the profile evidence directory.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Scroll the current browser tab.",
    "name": "browser_scroll",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMs": {
          "description": "Additional milliseconds to wait after scrolling. Default 300.",
          "type": "number"
        },
        "x": {
          "description": "Horizontal scroll offset in pixels. Default 0.",
          "type": "number"
        },
        "y": {
          "description": "Vertical scroll offset in pixels. Default 600 (one viewport down).",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Facade: composite one-call security research evidence workflow. Requires an already-open tab in the profile (run browser_open first). Internally calls capture start, snapshot, network summary, storage snapshot, cookie summary, token scan, and application export — then saves all artifacts and returns their paths. Returns {ok, artifactPaths, ...}. No manual capture setup needed before calling this; it starts its own capture window.",
    "name": "browser_security_pack",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "includeApplicationExport": {
          "description": "Include Application panel export artifact (storage/SW/IndexedDB). Default true.",
          "type": "boolean"
        },
        "includeHar": {
          "description": "Include a full HAR artifact. Default true.",
          "type": "boolean"
        },
        "includePerformanceHeavy": {
          "description": "Include expensive performance metrics (source map, coverage). Default false.",
          "type": "boolean"
        },
        "includeTokenScan": {
          "description": "Include a token/credential scan artifact. Default true.",
          "type": "boolean"
        },
        "includeTrace": {
          "description": "Include a Chrome performance trace artifact. Default false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum network records per evidence section. Default 20.",
          "type": "number"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "url": {
          "description": "Optional URL to navigate to before starting evidence collection.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMs": {
          "description": "Milliseconds to wait after navigation before collecting evidence. Default 2000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "One-call security research evidence workflow: optionally navigate, start capture, reload, collect agent_inspect routes, save HAR/Application/trace evidence, and return artifact paths.",
    "name": "browser_security_research_pack",
    "parameters": {
      "properties": {
        "includeApplicationExport": {
          "description": "Include Application panel export artifact (storage/SW/IndexedDB). Default true.",
          "type": "boolean"
        },
        "includeHar": {
          "description": "Include a full HAR artifact. Default true.",
          "type": "boolean"
        },
        "includePerformanceHeavy": {
          "description": "Include expensive performance metrics (source map, coverage). Default false.",
          "type": "boolean"
        },
        "includeTokenScan": {
          "description": "Include a token/credential scan artifact. Default true.",
          "type": "boolean"
        },
        "includeTrace": {
          "description": "Include a Chrome performance trace artifact. Default false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum network records per evidence section. Default 20.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "url": {
          "description": "Optional URL to navigate to before starting evidence collection.",
          "type": "string"
        },
        "waitMs": {
          "description": "Milliseconds to wait after navigation before collecting evidence. Default 2000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return current page security context and TLS/certificate summary for the profile tab.",
    "name": "browser_security_summary",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Set a select/radio/checkbox form control by CSS selector. For <select>, pass value, label, or index. For checkbox/radio, pass checked=true/false.",
    "name": "browser_select",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "checked": {
          "description": "For checkbox/radio: true to check, false to uncheck.",
          "type": "boolean"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "index": {
          "description": "For <select>: zero-based index of the option to select. Alternative to value/label.",
          "type": "number"
        },
        "label": {
          "description": "For <select>: the visible option label text to select. Alternative to value.",
          "type": "string"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "selector": {
          "description": "Required. CSS selector for the <select>, <input type=checkbox>, or <input type=radio> element.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "value": {
          "description": "For <select>: the option value attribute to select.",
          "type": "string"
        },
        "waitMs": {
          "description": "Additional milliseconds to wait after selection. Default 700, max 60000.",
          "type": "number"
        }
      },
      "required": [
        "selector"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return deeper Application panel Service Worker evidence: registrations, worker scripts, CacheStorage entries, and worker debugger targets.",
    "name": "browser_service_worker_detail",
    "parameters": {
      "properties": {
        "includeCacheEntries": {
          "description": "Include CacheStorage entries. Default true.",
          "type": "boolean"
        },
        "includeScripts": {
          "description": "Fetch and include worker script source text. Default true.",
          "type": "boolean"
        },
        "maxCacheEntries": {
          "description": "Maximum CacheStorage entries to return per cache. Default 50.",
          "type": "number"
        },
        "maxScriptChars": {
          "description": "Maximum characters per worker script to include. Default 120000.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return Application panel-style Service Worker and CacheStorage summary for the current profile tab.",
    "name": "browser_service_worker_summary",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return objective cross-panel browser signals across Network, Cookies, Storage, Service Workers, Security, and optional token scan. This does not decide vulnerability impact.",
    "name": "browser_signal_summary",
    "parameters": {
      "properties": {
        "includeTokenScan": {
          "description": "Also run browser_token_scan and include results. Default: false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Max items per network category. Default: 5.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return title, URL, visible text, and basic input/button inventory from the current tab. Hidden/aria-hidden controls are filtered and duplicates collapsed to keep the payload small; pass maxControls to change the cap. When page text exceeds 200 000 characters the full text is saved to disk and the response includes filePath + originalLength — use the Read tool on filePath to get the complete text.",
    "name": "browser_snapshot",
    "parameters": {
      "properties": {
        "maxControls": {
          "description": "Max controls to return after filtering/dedup. Default 40, max 200.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return JavaScript source for a scriptId in the current profile tab.",
    "name": "browser_source_get",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "scriptId": {
          "description": "Required. CDP script ID from browser_sources_list.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "required": [
        "scriptId"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return sourceMappingURL and source map metadata for a parsed JavaScript source.",
    "name": "browser_source_map_metadata",
    "parameters": {
      "properties": {
        "fetchMap": {
          "description": "Fetch external .map files over network. Default: false.",
          "type": "boolean"
        },
        "hasSourceMap": {
          "description": "Filter by source map presence.",
          "type": "boolean"
        },
        "ignoreCache": {
          "description": "Bypass cache on reload. Default: false.",
          "type": "boolean"
        },
        "isModule": {
          "description": "Filter by ES module type.",
          "type": "boolean"
        },
        "maxScripts": {
          "description": "Max scripts to process. Default: 120.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Only process scripts whose source contains this literal string.",
          "type": "string"
        },
        "reload": {
          "description": "Reload page to capture fresh script list. Default: true.",
          "type": "boolean"
        },
        "scriptId": {
          "description": "Select a specific script by CDP script ID.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "urlContains": {
          "description": "Filter scripts by URL substring (case-insensitive).",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after reload. Default: 8000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Read one saved original source file extracted from a source map, or extract and select one by script/source selector.",
    "name": "browser_source_map_source_get",
    "parameters": {
      "properties": {
        "fetchMap": {
          "description": "Fetch external .map files over network. Default: false.",
          "type": "boolean"
        },
        "hasSourceMap": {
          "description": "Filter by source map presence.",
          "type": "boolean"
        },
        "ignoreCache": {
          "description": "Bypass cache on reload. Default: false.",
          "type": "boolean"
        },
        "index": {
          "description": "Select a specific original source by 0-based index.",
          "type": "number"
        },
        "isModule": {
          "description": "Filter by ES module type.",
          "type": "boolean"
        },
        "maxChars": {
          "description": "Max characters of source content to return. Default: 120000.",
          "type": "number"
        },
        "maxScripts": {
          "description": "Max scripts to process. Default: 40.",
          "type": "number"
        },
        "maxSources": {
          "description": "Max original sources per script.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path to a previously saved source-map artifact file; bypasses live CDP extraction.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Only process scripts whose source contains this literal string.",
          "type": "string"
        },
        "reload": {
          "description": "Reload page to capture fresh script list. Default: true.",
          "type": "boolean"
        },
        "scriptId": {
          "description": "Select source by CDP script ID.",
          "type": "string"
        },
        "source": {
          "description": "Select a specific original source by name/path substring.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "urlContains": {
          "description": "Filter scripts by URL substring (case-insensitive).",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after reload. Default: 8000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Extract original source files from source maps and save them as profile-scoped evidence artifacts.",
    "name": "browser_source_map_sources",
    "parameters": {
      "properties": {
        "fetchMap": {
          "description": "Fetch external .map files over network. Default: false.",
          "type": "boolean"
        },
        "hasSourceMap": {
          "description": "Filter by source map presence.",
          "type": "boolean"
        },
        "ignoreCache": {
          "description": "Bypass cache on reload. Default: false.",
          "type": "boolean"
        },
        "isModule": {
          "description": "Filter by ES module type.",
          "type": "boolean"
        },
        "maxContentChars": {
          "description": "Max characters of source content to embed inline.",
          "type": "number"
        },
        "maxScripts": {
          "description": "Max scripts to process. Default: 40.",
          "type": "number"
        },
        "maxSources": {
          "description": "Max original sources to extract per script.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Only process scripts whose source contains this literal string.",
          "type": "string"
        },
        "reload": {
          "description": "Reload page to capture fresh script list. Default: true.",
          "type": "boolean"
        },
        "save": {
          "description": "Save extracted sources to disk. Default: true.",
          "type": "boolean"
        },
        "scriptId": {
          "description": "Select a specific script by CDP script ID.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "urlContains": {
          "description": "Filter scripts by URL substring (case-insensitive).",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after reload. Default: 8000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return a DevTools-style heuristic pretty-printed view of a parsed JavaScript source. When the pretty-printed source exceeds 200 000 characters the full content is saved to disk and the response includes filePath + originalLength — use the Read tool on filePath to get the complete source.",
    "name": "browser_source_pretty_print",
    "parameters": {
      "properties": {
        "hasSourceMap": {
          "description": "Filter by source map presence.",
          "type": "boolean"
        },
        "ignoreCache": {
          "description": "Bypass cache on reload. Default: false.",
          "type": "boolean"
        },
        "isModule": {
          "description": "Filter by ES module type.",
          "type": "boolean"
        },
        "maxScripts": {
          "description": "Max scripts to search through. Default: 120.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Only return a script whose raw source contains this literal string.",
          "type": "string"
        },
        "reload": {
          "description": "Reload page to capture fresh script list. Default: true.",
          "type": "boolean"
        },
        "scriptId": {
          "description": "Select a specific script by CDP script ID; otherwise first match wins.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "urlContains": {
          "description": "Filter scripts by URL substring (case-insensitive).",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after reload for scripts to parse. Default: 8000.",
          "type": "number"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return Sources panel-style script metadata for the current profile tab.",
    "name": "browser_sources_list",
    "parameters": {
      "properties": {
        "hasSourceMap": {
          "description": "Filter by source map presence (true = has map, false = no map).",
          "type": "boolean"
        },
        "isModule": {
          "description": "Filter by ES module type.",
          "type": "boolean"
        },
        "limit": {
          "description": "Max scripts to return. Default: 200.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "urlContains": {
          "description": "Filter scripts whose URL contains this substring (case-insensitive).",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Search parsed script sources for a literal query and return line/column snippets.",
    "name": "browser_sources_search",
    "parameters": {
      "properties": {
        "caseSensitive": {
          "description": "Case-sensitive search. Default: false.",
          "type": "boolean"
        },
        "contextChars": {
          "description": "Characters of context around each match.",
          "type": "number"
        },
        "hasSourceMap": {
          "description": "Filter by source map presence.",
          "type": "boolean"
        },
        "ignoreCache": {
          "description": "Bypass cache on reload. Default: false.",
          "type": "boolean"
        },
        "isModule": {
          "description": "Filter by ES module type.",
          "type": "boolean"
        },
        "maxMatches": {
          "description": "Max total matches to return. Default: 50.",
          "type": "number"
        },
        "maxScripts": {
          "description": "Max scripts to search. Default: 120.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "query": {
          "description": "Required. Literal string to search for in parsed script sources.",
          "type": "string"
        },
        "reload": {
          "description": "Reload page to capture fresh script list. Default: true.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "urlContains": {
          "description": "Restrict search to scripts whose URL contains this substring (case-insensitive).",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after reload. Default: 8000.",
          "type": "number"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return Application-panel origin evidence: frame origins, storage keys, usage/quota, and cookie partition metadata where Chrome exposes it.",
    "name": "browser_storage_origin_summary",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return localStorage, sessionStorage, document-visible cookies, and CDP cookies for the current profile tab.",
    "name": "browser_storage_snapshot",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return a normalized diagnosis of why the current page may be stuck: captcha, mfa, login wall, error, or no-page. Does not assess vulnerabilities.",
    "name": "browser_stuck",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Managed CDP: return browser/system information from SystemInfo.getInfo where Chrome exposes it.",
    "name": "browser_system_info",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "description": "Close browser tabs. Managed backend closes all pages in the given profile context (one profile = one tab in normal usage). Personal backend closes a specific tab by tabId from the user's real Chrome. Pass tabId for personal; managed only needs profile. Required cleanup at end of any browser flow so profiles do not accumulate orphan tabs.",
    "name": "browser_tab_close",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Managed profile name. Ignored for personal.",
          "type": "string"
        },
        "tabId": {
          "description": "Personal backend: Chrome tab id to close. Ignored for managed.",
          "type": [
            "string",
            "number"
          ]
        },
        "tabIds": {
          "description": "Personal backend: batch close multiple tab ids.",
          "items": {
            "type": [
              "string",
              "number"
            ]
          },
          "type": "array"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "List visible browser tabs available through the local CDP endpoint and show which durable profiles are attached or stale. Use browser_tab_close to close a tab; this tool does not accept action/close/profile parameters (profile param is silently ignored). Returns top-level `tabs` array on both managed and personal backends; `staleProfiles` and `summary` are null on personal (profile registry is in the worker).",
    "name": "browser_tabs",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "description": "Facade: extract readable text content from the current page in one call. Returns plain text, page URL, title, and character count. Use instead of browser_eval + document.body.innerText for common text extraction. When the page text exceeds 200 000 characters the full content is saved to disk and the response includes filePath + originalLength — use the Read tool on filePath to get the complete text.",
    "name": "browser_text",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Temporarily instrument fetch, XHR, local/session storage, and document.cookie to capture objective token-like data flow evidence during a trigger.",
    "name": "browser_token_flow_trace",
    "parameters": {
      "properties": {
        "durationMs": {
          "description": "Observation window in ms. Default: 1000, max 10000.",
          "type": "number"
        },
        "includeValues": {
          "description": "Include actual token values in output. Default: true.",
          "type": "boolean"
        },
        "maxEvents": {
          "description": "Max token-flow events to capture. Default: 100.",
          "type": "number"
        },
        "maxValueChars": {
          "description": "Max characters per captured value. Default: 4000.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "tabId": {
          "description": "CDP tab ID. Defaults to the profile's current tab.",
          "type": "string"
        },
        "triggerExpression": {
          "description": "JS expression to evaluate to trigger token flow during observation.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Scan managed browser Network, storage, and cookies for token-like material. Returns full values in authorized runtime mode.",
    "name": "browser_token_scan",
    "parameters": {
      "properties": {
        "limit": {
          "description": "Max traffic records to scan. Default: 500.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Agent usability: list available tools by category, description, required fields, and parameter names so agents do not choose blindly.",
    "name": "browser_tool_catalog",
    "parameters": {
      "properties": {
        "category": {
          "description": "Filter by tool category (e.g. 'network', 'storage', 'composite').",
          "type": "string"
        },
        "includeBackendSpecific": {
          "description": "Include tools specific to managed-cdp backend. Default true.",
          "type": "boolean"
        },
        "query": {
          "description": "Literal substring filter applied to tool names and descriptions.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Agent usability: return description, parameters, category, and small usage hints for one tool.",
    "name": "browser_tool_help",
    "parameters": {
      "properties": {
        "tool": {
          "description": "Required. Exact tool name to get help for (e.g. 'browser_navigate', 'browser_tool_catalog').",
          "type": "string"
        }
      },
      "required": [
        "tool"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compare two saved Chrome trace JSON files and report objective differences in event names, categories, phases, threads, and duration buckets.",
    "name": "browser_trace_compare",
    "parameters": {
      "properties": {
        "afterTracePath": {
          "description": "Absolute path to the after-state Chrome trace JSON. Defaults to newest trace for the profile.",
          "type": "string"
        },
        "beforeTracePath": {
          "description": "Absolute path to the before-state Chrome trace JSON. Defaults to second-newest trace for the profile.",
          "type": "string"
        },
        "limit": {
          "description": "Max diff entries to return per category.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name used to find default trace paths when beforeTracePath/afterTracePath are omitted.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Query a saved Chrome trace JSON file by event name, category, phase, duration, thread, or time range without loading the full trace into the agent context.",
    "name": "browser_trace_query",
    "parameters": {
      "properties": {
        "category": {
          "description": "Filter events by category substring.",
          "type": "string"
        },
        "endTimeMs": {
          "description": "End of time window in ms from trace origin.",
          "type": "number"
        },
        "limit": {
          "description": "Max events to return.",
          "type": "number"
        },
        "maxDurationMs": {
          "description": "Maximum event duration in ms (inclusive).",
          "type": "number"
        },
        "minDurationMs": {
          "description": "Minimum event duration in ms (inclusive).",
          "type": "number"
        },
        "name": {
          "description": "Filter events by exact name.",
          "type": "string"
        },
        "phase": {
          "description": "Filter events by CDP trace phase code (e.g. 'B', 'E', 'X').",
          "type": "string"
        },
        "processId": {
          "description": "Filter events by process ID.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name used to locate the default trace path.",
          "type": "string"
        },
        "query": {
          "description": "Filter events whose name or category contains this string.",
          "type": "string"
        },
        "sortBy": {
          "description": "Sort order for results. Values: duration, timestamp, name.",
          "enum": [
            "duration",
            "timestamp",
            "name"
          ],
          "type": "string"
        },
        "startTimeMs": {
          "description": "Start of time window in ms from trace origin.",
          "type": "number"
        },
        "threadId": {
          "description": "Filter events by thread ID.",
          "type": "number"
        },
        "tracePath": {
          "description": "Absolute path to the trace JSON file. Defaults to the most recent trace for the profile.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Type text into a field by simulating individual keystrokes (Playwright keyboard events). Required param: selector (CSS), text. Keystrokes trigger keydown/keypress/keyup events and do NOT clear the field first. Pass clear=true to replace the full value atomically (triple-click clears before typing — preferred for React controlled inputs). Set pressEnter=true to submit after typing.",
    "name": "browser_type",
    "parameters": {
      "properties": {
        "actionTimeoutMs": {
          "description": "How long to wait for the field to become focusable/editable. Default 3000 ms, max 60000.",
          "type": "number"
        },
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "clear": {
          "description": "If true, triple-clicks to clear the field before typing. Default false.",
          "type": "boolean"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "frameIndexes": {
          "description": "Optional. Frame index path for nested iframe targeting.",
          "items": {
            "type": "number"
          },
          "type": "array"
        },
        "framePath": {
          "description": "Optional. Frame path for input inside a specific iframe.",
          "type": "string"
        },
        "includeFrames": {
          "description": "Not implemented — driver ignores this flag. To target elements inside an iframe use framePath (iframe CSS selector string, e.g. 'iframe#chat') or frameIndexes (0-based integer array, e.g. [0] for the first iframe).",
          "type": "boolean"
        },
        "includeShadow": {
          "description": "Advisory — not consumed by driver code. Playwright auto-traverses open shadow roots. For explicit shadow-root element targeting use a CSS selector with Playwright's >> deep-piercing syntax.",
          "type": "boolean"
        },
        "maxShadowRoots": {
          "description": "Advisory — not consumed by driver code. Playwright handles shadow DOM traversal natively with no configurable limit.",
          "type": "number"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "pressEnter": {
          "description": "If true, presses Enter after typing to submit the form. Default false.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "selector": {
          "description": "Required. CSS selector for the input field (e.g. \"input[name='email']\", \"#password\").",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "text": {
          "description": "Required. Text to type into the field character by character.",
          "type": "string"
        },
        "timeoutMs": {
          "description": "Alias for actionTimeoutMs on the pre-type focus wait.",
          "type": "number"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        }
      },
      "required": [
        "selector",
        "text"
      ],
      "type": "object"
    }
  },
  {
    "description": "Set files on an input[type=file] by CSS selector, similar to Playwright setInputFiles. Use for upload widgets after finding the file input.",
    "name": "browser_upload",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "file": {
          "description": "Single absolute file path to set on the input. Alternative to files array.",
          "type": "string"
        },
        "files": {
          "description": "Array of absolute file paths to set on the input. Use file for a single file.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "selector": {
          "description": "Required. CSS selector for the file input element (e.g. \"input[type=file]\").",
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        },
        "waitMs": {
          "description": "Additional milliseconds to wait after setting files. Default 700, max 60000.",
          "type": "number"
        }
      },
      "required": [
        "selector"
      ],
      "type": "object"
    }
  },
  {
    "description": "Wait for a page condition: selector visible/attached/hidden/detached, visible text, URL substring, captured network request, or a plain timeout.",
    "name": "browser_wait",
    "parameters": {
      "properties": {
        "backend": {
          "description": "Browser backend. Use personal/currentTab for the user's already-open Chrome; use managed for isolated CDP profiles.",
          "enum": [
            "managed",
            "managed-cdp",
            "personal",
            "personal-chrome",
            "auto"
          ],
          "type": "string"
        },
        "currentTab": {
          "description": "Route to Personal Chrome's active tab when available.",
          "type": "boolean"
        },
        "personal": {
          "description": "Shortcut for backend=personal.",
          "type": "boolean"
        },
        "pollMs": {
          "description": "Polling interval in milliseconds for request/URL conditions. Default 500, min 50.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the active sticky-bound profile if omitted.",
          "type": "string"
        },
        "requestMethod": {
          "description": "Optional HTTP method filter for requestUrlContains (e.g. \"POST\").",
          "type": "string"
        },
        "requestStatus": {
          "description": "Optional HTTP status code filter for requestUrlContains.",
          "type": "number"
        },
        "requestUrlContains": {
          "description": "Wait until captured Network traffic contains a request URL matching this substring.",
          "type": "string"
        },
        "selector": {
          "description": "CSS selector to wait for (used with state). Omit to wait by text/url/request instead.",
          "type": "string"
        },
        "state": {
          "description": "Element state to wait for when using selector. Default \"visible\".",
          "enum": [
            "visible",
            "attached",
            "hidden",
            "detached"
          ],
          "type": "string"
        },
        "tabId": {
          "description": "Optional. Tab id override.",
          "type": "string"
        },
        "text": {
          "description": "Visible text to wait for on the page.",
          "type": "string"
        },
        "timeoutMs": {
          "description": "Maximum milliseconds to wait before giving up. Default 8000, clamped to 60000.",
          "type": "number"
        },
        "urlContains": {
          "description": "Wait until the current page URL contains this substring.",
          "type": "string"
        },
        "useCurrentTab": {
          "description": "Alias for currentTab.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Deep-dive frame, iframe, worker, Service Worker, CacheStorage, and target evidence so agents can inspect execution boundaries.",
    "name": "browser_worker_frame_deep_dive",
    "parameters": {
      "properties": {
        "includeServiceWorkerDetail": {
          "description": "If false, skip Service Worker detail fetch (faster). Default true.",
          "type": "boolean"
        },
        "path": {
          "description": "Absolute path to save the report JSON. Auto-generated in evidence/boundaries/ if omitted.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile.",
          "type": "string"
        },
        "save": {
          "description": "If true, persist the deep-dive report JSON to disk.",
          "type": "boolean"
        },
        "tabId": {
          "description": "CDP tab ID override. Defaults to the profile's current tab.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Agent usability: return deterministic tool recipes for common browser-security research tasks.",
    "name": "browser_workflow_guide",
    "parameters": {
      "properties": {
        "task": {
          "description": "Task recipe to retrieve (e.g. 'professional-appsec', 'login-logout', 'cors'). Returns all recipes if omitted.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "控制 browser profile 的 Fetch domain 拦截。action: start（标记 URL pattern 开始拦截）/ list（列当前暂停的请求）/ continue（放行，可选改 URL/method/header/body）/ fail（模拟网络失败）。",
    "name": "cdp_fetch_intercept",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "action": {
          "description": "动作：start（开始拦截）/ list（列暂停）/ continue（放行，可改 header/body/url/method）/ fail（模拟失败）",
          "enum": [
            "start",
            "list",
            "continue",
            "fail"
          ],
          "type": "string"
        },
        "body": {
          "description": "改写请求 body，工具会按 CDP 要求转 base64（action=continue 时用）",
          "type": "string"
        },
        "body_base64": {
          "description": "已 base64 编码的请求 body（action=continue 时用）",
          "type": "string"
        },
        "captured_request_id": {
          "description": "capturedRequestId（action=continue/fail 时用，来自 list 返回）",
          "type": "string"
        },
        "error_reason": {
          "default": "BlockedByClient",
          "description": "CDP errorReason（action=fail 时用）",
          "type": "string"
        },
        "header_overrides": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "header 修改 {name:value}（action=continue 时用）",
          "type": "object"
        },
        "json": {
          "description": "把对象序列化成 JSON 请求 body（action=continue 时用）"
        },
        "method": {
          "description": "改写请求 method（action=continue 时用）",
          "type": "string"
        },
        "profile": {
          "description": "Browser profile name (lowercase). Naming convention '<target>-<identity>' or the older '<target>-<slot>' (e.g. 'app-merchant-a' / 'app-payer' / 'app-1'), each with isolated cookies/storage. Configured under browser.profiles in browser-profiles.json. Open a page with that profile using the browser tools first, then query the CDP capture.",
          "minLength": 1,
          "type": "string"
        },
        "remove_headers": {
          "description": "要删除的 header 名称（action=continue 时用）",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "url": {
          "description": "改写请求 URL（action=continue 时用）",
          "type": "string"
        },
        "url_pattern": {
          "description": "URL substring 匹配（action=start 时用，空串=所有）",
          "type": "string"
        }
      },
      "required": [
        "profile",
        "action"
      ],
      "type": "object"
    }
  },
  {
    "description": "按 type 查询 browser profile 的各类 CDP 捕获数据。type: traffic/audits/console/dialogs/exceptions/security/storage/frames/scripts/dom_mutations/webauthn/fetch。不同 type 支持不同 filter 字段（如 traffic 支持 url_contains/method/status，console 支持 log_type/text_contains）。",
    "name": "cdp_query",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "after": {
          "description": "ISO 8601 时间戳增量过滤，**必须带时区**（如 '2026-04-28T10:00:00Z'）。无时区会按本地解析有漂移风险",
          "type": "string"
        },
        "before": {
          "description": "ISO 8601 时间戳上界，配合 after 拿 [after, before] 时间窗口",
          "type": "string"
        },
        "blocked_reason_contains": {
          "description": "Substring 匹 blockedReason（case-insensitive）。CDP 标准值如：\n'csp' / 'mixed-content' / 'origin' / 'inspector' / 'subresource-filter'\n/ 'content-type' / 'collapsed-by-client' / 'coep-frame-resource-needs-coep-header'\n/ 'corp-not-same-origin' / 'corp-not-same-site' / 'corp-not-same-origin-after-defaulted-to-same-origin-by-coep'\n/ 'unsupported'",
          "type": "string"
        },
        "dialog_type": {
          "description": "CDP javascriptDialog type（严格匹）",
          "enum": [
            "alert",
            "confirm",
            "prompt",
            "beforeunload"
          ],
          "type": "string"
        },
        "failed_only": {
          "description": "仅返回 failed=true 的请求（包括 blocked / network error / canceled）",
          "type": "boolean"
        },
        "frame_type": {
          "description": "Page.frame* event type（严格匹）",
          "enum": [
            "navigated",
            "attached",
            "detached",
            "startedLoading",
            "stoppedLoading"
          ],
          "type": "string"
        },
        "frame_url_contains": {
          "description": "Substring 匹 url（case-insensitive）。如要按 hostname 精准匹用 hostname 字段",
          "type": "string"
        },
        "from_cache": {
          "description": "仅返回 fromCache=true 的请求（cache hit，未真发到服务器）",
          "type": "boolean"
        },
        "has_source_map": {
          "description": "仅返回 sourceMapURL 非空的脚本（true）/ 仅返回无 sourcemap 的（false）",
          "type": "boolean"
        },
        "hostname": {
          "description": "精确按 url 的 hostname 匹（防止 query string 误中），如 'example.com'",
          "type": "string"
        },
        "is_module": {
          "description": "仅返回 ES module 脚本（true）/ 仅返回普通脚本（false）",
          "type": "boolean"
        },
        "issue_code": {
          "description": "Substring 匹配 issueCode（case-insensitive，**先 toLowerCase 再 includes**）。\nCDP 当前标准值（22 个）：CookieIssue / MixedContentIssue / BlockedByResponseIssue\n  / HeavyAdIssue / ContentSecurityPolicyIssue / SharedArrayBufferIssue\n  / LowTextContrastIssue / CorsIssue / AttributionReportingIssue\n  / QuirksModeIssue / GenericIssue / DeprecationIssue / ClientHintIssue\n  / FederatedAuthRequestIssue / BounceTrackingIssue / StylesheetLoadingIssue\n  / NavigatorUserAgentIssue / PropertyRuleIssue / SameSiteCookieIssue\n  / TrustedWebActivityIssue / WasmCrossOriginModuleSharingIssue\n  / FederatedAuthUserInfoRequestIssue。\n**'CSP' 不会匹** 'ContentSecurityPolicyIssue'，要写 'ContentSecurityPolicy' 或 'PolicyIssue'",
          "type": "string"
        },
        "key_contains": {
          "description": "storage key substring",
          "type": "string"
        },
        "limit": {
          "description": "返回**最新** N 条（slice(-limit) 取尾部），默认 50。如需更早数据用 before 时间过滤翻页",
          "exclusiveMinimum": 0,
          "maximum": 10000,
          "type": "integer"
        },
        "log_type": {
          "description": "CDP console type（**严格匹**），CDP 用完整词如 'warning' 不是 'warn'",
          "enum": [
            "log",
            "debug",
            "info",
            "error",
            "warning",
            "dir",
            "dirxml",
            "table",
            "trace",
            "clear",
            "startGroup",
            "startGroupCollapsed",
            "endGroup",
            "assert",
            "profile",
            "profileEnd",
            "count",
            "timeEnd"
          ],
          "type": "string"
        },
        "method": {
          "description": "HTTP 方法（自动 toUpperCase 严格匹）。单值精确匹，如要 OR 多个方法请发多次调用",
          "enum": [
            "GET",
            "POST",
            "PUT",
            "DELETE",
            "PATCH",
            "HEAD",
            "OPTIONS",
            "CONNECT",
            "TRACE"
          ],
          "type": "string"
        },
        "mutation_target_contains": {
          "description": "Substring 匹 target 选择器（如 'button' / '#login' / '.modal'）",
          "type": "string"
        },
        "mutation_type": {
          "description": "MutationRecord.type（严格匹）：childList / attributes / characterData",
          "enum": [
            "childList",
            "attributes",
            "characterData"
          ],
          "type": "string"
        },
        "origin": {
          "description": "storage origin 过滤",
          "type": "string"
        },
        "profile": {
          "description": "Browser profile name (lowercase). Naming convention '<target>-<identity>' or the older '<target>-<slot>' (e.g. 'app-merchant-a' / 'app-payer' / 'app-1'), each with isolated cookies/storage. Configured under browser.profiles in browser-profiles.json. Open a page with that profile using the browser tools first, then query the CDP capture.",
          "minLength": 1,
          "type": "string"
        },
        "resource_type": {
          "description": "CDP Network.ResourceType（PascalCase 严格匹），如 'Document' / 'Script' / 'XHR' / 'Fetch' / 'WebSocket' / 'CSPViolationReport' / 'Preflight'",
          "enum": [
            "Document",
            "Stylesheet",
            "Image",
            "Media",
            "Font",
            "Script",
            "TextTrack",
            "XHR",
            "Fetch",
            "Prefetch",
            "EventSource",
            "WebSocket",
            "Manifest",
            "SignedExchange",
            "Ping",
            "CSPViolationReport",
            "Preflight",
            "Other"
          ],
          "type": "string"
        },
        "script_url_contains": {
          "type": "string"
        },
        "state": {
          "description": "CDP Security state（严格匹）。注：'info' 在新 Chrome 已 deprecated，几乎不出现",
          "enum": [
            "unknown",
            "neutral",
            "insecure",
            "secure",
            "info",
            "insecure-broken"
          ],
          "type": "string"
        },
        "status": {
          "description": "HTTP 状态码精确匹单值（如 400 / 401 / 500）。要范围如 4xx 请多次调用或外部过滤",
          "type": "integer"
        },
        "storage_type": {
          "description": "Substring 匹配 type（case-insensitive）。完整 6 个动作粒度：\n'indexedDB.contentUpdated' / 'cacheStorage.contentUpdated'\n/ 'domStorage.itemAdded' / 'domStorage.itemUpdated'\n/ 'domStorage.itemRemoved' / 'domStorage.cleared'。\n按大类查用 'indexedDB' / 'cacheStorage' / 'domStorage'",
          "type": "string"
        },
        "text_contains": {
          "description": "Substring 匹（case-insensitive）。注：对象类参数（如 console.log({token:'abc'})）只能匹到 description='Object'，匹不到对象内部字段",
          "type": "string"
        },
        "type": {
          "description": "查询类型：traffic/audits/console/dialogs/exceptions/security/storage/frames/scripts/dom_mutations/webauthn/fetch",
          "enum": [
            "traffic",
            "audits",
            "console",
            "dialogs",
            "exceptions",
            "security",
            "storage",
            "frames",
            "scripts",
            "dom_mutations",
            "webauthn",
            "fetch"
          ],
          "type": "string"
        },
        "url_contains": {
          "description": "Substring 匹 url（case-insensitive）。如要按 hostname 精准匹用 hostname 字段",
          "type": "string"
        },
        "webauthn_type": {
          "type": "string"
        }
      },
      "required": [
        "profile",
        "type"
      ],
      "type": "object"
    }
  },
  {
    "description": "Create or reopen a durable agent browser profile. A profile owns one tab and one evidence directory.",
    "name": "profile_create",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Required. Profile name (slug, e.g. 'target-clean').",
          "type": "string"
        },
        "url": {
          "description": "URL to open in the new profile. Omit to create metadata-only without a browser tab.",
          "type": "string"
        }
      },
      "required": [
        "profile"
      ],
      "type": "object"
    }
  },
  {
    "description": "Delete a managed browser profile record, close its tab, and remove the profile directory (including evidence files) from disk.",
    "name": "profile_delete",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Required. Profile name to delete.",
          "type": "string"
        }
      },
      "required": [
        "profile"
      ],
      "type": "object"
    }
  },
  {
    "description": "Export profile-local managed browser network traffic as a HAR-like object.",
    "name": "profile_export_har",
    "parameters": {
      "properties": {
        "includeBodies": {
          "description": "Include response bodies in the HAR entries. Default: false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Max traffic records to export. Default: 1000.",
          "type": "number"
        },
        "maxBodyBytes": {
          "description": "Max bytes per response body when includeBodies=true. Default: 200000.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Report objective HAR evidence completeness for captured traffic: bodies, truncation, timing phases, redirects, and security details.",
    "name": "profile_har_completeness",
    "parameters": {
      "properties": {
        "includeBodies": {
          "description": "Include response bodies in the analysis. Default: false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Max traffic records to analyze. Default: 1000.",
          "type": "number"
        },
        "maxBodyBytes": {
          "description": "Max bytes per response body. Default: 200000.",
          "type": "number"
        },
        "maxRows": {
          "description": "Max rows in the completeness report. Default: 50.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path for the saved report. Defaults to evidence/har/<timestamp>-har-completeness.json.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "save": {
          "description": "Save the completeness report to disk. Default: true.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Forge JWT auth-bypass candidates from an original token — pure offline algorithm (no network, no Chrome). Generates the classic attack families: alg=none (unsigned, with none/None/NONE spellings), weak HS256 secret (cracks the original signature against a wordlist then re-signs), RS256->HS256 alg confusion (signs with the RSA public key PEM as HMAC secret), kid header injection (path-traversal / SQLi / command payloads), and jku/x5u JWKS spoofing — optionally with payload claim mutations for privilege escalation (e.g. role=admin, sub=0). Returns only the constructed candidate tokens plus objective build notes; it does NOT judge whether the target accepts a token or whether privilege was escalated — the agent replays each candidate and classifies.",
    "name": "profile_jwt_forge",
    "parameters": {
      "properties": {
        "attackerJku": {
          "description": "Attacker-controlled URL substituted into the jku/x5u headers; defaults to a placeholder when omitted.",
          "type": "string"
        },
        "attacks": {
          "description": "Which variant families to generate. Default: all.",
          "items": {
            "enum": [
              "none",
              "weak-secret",
              "hs-confusion",
              "kid-injection",
              "jku-spoof",
              "x5u-spoof"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "mutations": {
          "description": "Claim overrides shallow-merged into the payload, e.g. { \"role\": \"admin\", \"sub\": \"0\" } for privilege escalation.",
          "type": "object"
        },
        "publicKeyPem": {
          "description": "RSA/EC public key PEM (the key the target verifies RS256/ES256 with); required for the hs-confusion variant.",
          "type": "string"
        },
        "secretWordlist": {
          "description": "HS256 weak-secret candidates. Default: a small built-in list (secret/password/123456/changeme/jwt/key/admin/test/secretkey ...).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "token": {
          "description": "The original JWT (header.payload.signature) to forge variants from.",
          "type": "string"
        }
      },
      "required": [
        "token"
      ],
      "type": "object"
    }
  },
  {
    "description": "List durable agent browser profiles managed by this local server, including whether each profile is attached to a live CDP tab.",
    "name": "profile_list",
    "parameters": {
      "properties": {},
      "type": "object"
    }
  },
  {
    "description": "Return F12 Network Timing/Initiator-style rows for captured managed browser requests.",
    "name": "profile_network_timeline",
    "parameters": {
      "properties": {
        "failed": {
          "description": "Filter: if true, return only failed requests.",
          "type": "boolean"
        },
        "from_cache": {
          "description": "Filter: if true, return only cache-served responses.",
          "type": "boolean"
        },
        "from_service_worker": {
          "description": "Filter: if true, return only service-worker-handled responses.",
          "type": "boolean"
        },
        "has_request_body": {
          "description": "Filter: if true, return only requests with a captured request body.",
          "type": "boolean"
        },
        "has_response_body": {
          "description": "Filter: if true, return only requests with a captured response body.",
          "type": "boolean"
        },
        "hostname": {
          "description": "Filter: return only requests to this hostname.",
          "type": "string"
        },
        "limit": {
          "description": "Maximum timeline rows to return. Default 100.",
          "type": "number"
        },
        "method": {
          "description": "Filter: return only requests with this HTTP method.",
          "type": "string"
        },
        "mime_contains": {
          "description": "Filter: return only responses whose MIME type contains this substring.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile if omitted.",
          "type": "string"
        },
        "redirected": {
          "description": "Filter: if true, return only redirected requests.",
          "type": "boolean"
        },
        "request_header": {
          "description": "Filter: return only requests containing this header.",
          "type": "object"
        },
        "resource_type": {
          "description": "Filter: return only requests of this resource type.",
          "type": "string"
        },
        "response_header": {
          "description": "Filter: return only responses containing this header.",
          "type": "object"
        },
        "sort_by": {
          "description": "Field to sort by. Default: timestamp.",
          "type": "string"
        },
        "sort_dir": {
          "description": "Sort direction. Default \"asc\".",
          "enum": [
            "asc",
            "desc"
          ],
          "type": "string"
        },
        "status": {
          "description": "Filter: return only requests with this exact HTTP status code.",
          "type": "number"
        },
        "status_max": {
          "description": "Filter: return only requests with status <= this value.",
          "type": "number"
        },
        "status_min": {
          "description": "Filter: return only requests with status >= this value.",
          "type": "number"
        },
        "url_contains": {
          "description": "Filter: return only requests whose URL contains this substring.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Mint a unique out-of-band (OOB) callback URL/domain to plant into a payload for catching BLIND vulnerabilities. Three modes: (1) http (default) — returns a plain callback URL, embed in SSRF/SSTI/XXE/log4j payloads; (2) redirect — returns a base redirect URL (append ?to=<URL-encoded-target>) so the server records the callback then 302s the target to a second URL (SSRF chain: proves out-of-band reach AND redirects to internal endpoint); (3) dns — returns a DNS exfil domain (<token>.exfil.YOUR-DOMAIN.example), embed as DNS query target to exfiltrate data via DNS labels. All modes share the same ring buffer; use profile_oob_poll with the token to read callbacks (protocol field distinguishes http vs dns entries). Server priority: params.serverBase > env OOB_SERVER_BASE > public default. Only mints an identifier — whether a callback proves a blind vuln is the agent's call, NOT a vulnerability judgment.",
    "name": "profile_oob_alloc",
    "parameters": {
      "properties": {
        "mode": {
          "description": "Callback mode. http (default): plain URL for SSRF/XXE/log4j. redirect: URL base ending in ?to= — append URL-encoded target, server records callback then 302s to target (SSRF chain). dns: DNS exfil domain <token>.exfil.YOUR-DOMAIN.example — embed as DNS query target, send data in label prefix.",
          "enum": [
            "http",
            "redirect",
            "dns"
          ],
          "type": "string"
        },
        "serverBase": {
          "description": "Collector base URL (e.g. http://your-collector.example). Overrides env OOB_SERVER_BASE. If neither is set, a non-functional placeholder is used and poll will report it.",
          "type": "string"
        },
        "token": {
          "description": "Optional fixed token; default is a random crypto token. Sanitised to [A-Za-z0-9_-].",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Read back the objective callbacks an OOB token has received from the configurable collector (GET {base}/__oob/poll). Returns interactionCount + interactions (source_ip, protocol, method, path, timestamp, user_agent, raw_headers) plus signals (http_count, dns_only_count, distinct_source_ips). A recorded callback means something fetched the planted URL; whether that proves blind SSRF/SSTI/XXE/log4j is the agent's classification — NOT a vulnerability judgment. Unreachable / timed-out collector returns a structured { error } object instead of throwing.",
    "name": "profile_oob_poll",
    "parameters": {
      "properties": {
        "limit": {
          "description": "Max records to return (1-1000, default 100).",
          "type": "number"
        },
        "serverBase": {
          "description": "Collector base URL; overrides env OOB_SERVER_BASE. Must match the base the token was alloc'd against.",
          "type": "string"
        },
        "since": {
          "description": "ISO timestamp; only return callbacks strictly after it (incremental polling).",
          "type": "string"
        },
        "timeoutMs": {
          "description": "Poll request timeout in ms (default 8000).",
          "type": "number"
        },
        "token": {
          "description": "The token returned by profile_oob_alloc.",
          "type": "string"
        }
      },
      "required": [
        "token"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fire N (>=2) byte-exact HTTP requests concurrently over independent raw node sockets to drive a TOCTOU / race-condition test (coupon reuse, double-spend, invite/quota bypass, purchase-limit bypass). last-byte sync (default) holds back each request's final byte until ALL sockets have their head flushed, then releases all final bytes in one tight loop (Turbo Intruder gate technique) so the requests contend in a single tight window; parallel mode just Promise.all's the full requests as a fallback. Returns objective signals only — per-request status/headers/bytes, first-byte timing relative to a shared t0, status distribution, and first-byte spread (smaller = tighter race window). It does NOT judge whether the race won; the agent classifies from the status distribution and timing.",
    "name": "profile_race_request",
    "parameters": {
      "properties": {
        "host": {
          "description": "Required. Target hostname or IP.",
          "type": "string"
        },
        "maxResponseBytes": {
          "description": "Max response bytes per request. Default: 524288 (512 KB), clamped to 67108864 (64 MB).",
          "type": "number"
        },
        "port": {
          "description": "TCP port. Default: 443 when tls=true, 80 otherwise.",
          "type": "number"
        },
        "readTimeoutMs": {
          "description": "Socket read timeout per request in ms. Default: 8000, clamped to 60000.",
          "type": "number"
        },
        "requests": {
          "description": "Required. N (>=2) requests to race. Each is { rawRequest } or { rawRequestBase64 } with exact bytes; put CRLF (\\r\\n) verbatim. They may be identical (e.g. replay the same coupon N times) or differ.",
          "items": {
            "properties": {
              "rawRequest": {
                "description": "Exact request bytes for this entry.",
                "type": "string"
              },
              "rawRequestBase64": {
                "description": "Base64 request bytes; takes precedence over rawRequest.",
                "type": "string"
              }
            },
            "type": "object"
          },
          "minItems": 2,
          "type": "array"
        },
        "servername": {
          "description": "SNI hostname for TLS. Defaults to host.",
          "type": "string"
        },
        "syncMode": {
          "description": "last-byte (default) = single-packet/gate sync; parallel = plain concurrent send.",
          "enum": [
            "last-byte",
            "parallel"
          ],
          "type": "string"
        },
        "tls": {
          "description": "Use TLS. Default: false.",
          "type": "boolean"
        }
      },
      "required": [
        "host",
        "requests"
      ],
      "type": "object"
    }
  },
  {
    "description": "Send a byte-exact HTTP request over a raw node socket (net/tls), bypassing Chrome's network stack. Unlike request_replay (which runs fetch() in the page and is normalised by Chrome), this sends malformed framing verbatim — dual Content-Length, CL.TE/TE.CL desync, pipelined requests — for HTTP request smuggling and protocol-level testing. Returns raw response bytes plus objective signals (httpResponseCount, timing, closeReason); it does NOT judge whether desync occurred (the agent classifies).",
    "name": "profile_raw_request",
    "parameters": {
      "properties": {
        "host": {
          "description": "Required. Target hostname or IP.",
          "type": "string"
        },
        "maxResponseBytes": {
          "description": "Max response bytes to read before closing. Default: 524288 (512 KB), clamped to 67108864 (64 MB).",
          "type": "number"
        },
        "port": {
          "description": "TCP port. Default: 443 when tls=true, 80 otherwise.",
          "type": "number"
        },
        "rawRequest": {
          "description": "Exact request bytes; put CRLF (\\r\\n) and any malformed framing here verbatim.",
          "type": "string"
        },
        "rawRequestBase64": {
          "description": "Base64 request bytes for non-UTF8/binary framing; takes precedence over rawRequest.",
          "type": "string"
        },
        "readTimeoutMs": {
          "description": "Socket read timeout in ms. Default: 8000, clamped to 60000.",
          "type": "number"
        },
        "servername": {
          "description": "SNI hostname for TLS. Defaults to host.",
          "type": "string"
        },
        "tls": {
          "description": "Use TLS. Default: false.",
          "type": "boolean"
        }
      },
      "required": [
        "host"
      ],
      "type": "object"
    }
  },
  {
    "description": "Return F12 Network real-time channel evidence: WebSocket lifecycle/frames and EventSource/SSE messages.",
    "name": "profile_realtime_log",
    "parameters": {
      "properties": {
        "direction": {
          "description": "Filter frames by direction: send or receive.",
          "enum": [
            "send",
            "receive"
          ],
          "type": "string"
        },
        "limit": {
          "description": "Max channels and frames to return. Default: 100.",
          "type": "number"
        },
        "maxPayloadChars": {
          "description": "Max characters per frame payload. Default: 2000.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path for the saved report. Defaults to evidence/realtime/<timestamp>-realtime-log.json.",
          "type": "string"
        },
        "payload_contains": {
          "description": "Filter to channels that have at least one frame containing this string.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "requestId": {
          "description": "Filter to a specific WebSocket/SSE request ID.",
          "type": "string"
        },
        "save": {
          "description": "Save the report as a JSON file to the evidence directory.",
          "type": "boolean"
        },
        "url_contains": {
          "description": "Filter channels whose URL contains this substring (case-insensitive).",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Return F12 request-detail evidence for one captured managed browser request: headers, cookies, timing, initiator, redirects, and body availability.",
    "name": "profile_request_detail",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "requestId": {
          "description": "Required. Request ID from profile_traffic_query.",
          "type": "string"
        }
      },
      "required": [
        "requestId"
      ],
      "type": "object"
    }
  },
  {
    "description": "Get request payload/postData for a captured managed browser requestId when CDP still has it available.",
    "name": "profile_request_payload",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "requestId": {
          "description": "Required. Request ID from profile_traffic_query.",
          "type": "string"
        }
      },
      "required": [
        "requestId"
      ],
      "type": "object"
    }
  },
  {
    "description": "Replay a captured managed browser request with optional URL, method, headers, and body overrides.",
    "name": "profile_request_replay",
    "parameters": {
      "properties": {
        "body": {
          "description": "Override the request body as a raw string.",
          "type": "string"
        },
        "credentials": {
          "description": "Fetch credentials mode. Default: include.",
          "type": "string"
        },
        "form": {
          "description": "Override body as URL-encoded form fields.",
          "type": "object"
        },
        "headers": {
          "description": "Headers to add or override (key: value pairs; null value removes a header).",
          "type": "object"
        },
        "json": {
          "description": "Override body with this value serialized as JSON."
        },
        "method": {
          "description": "Override the HTTP method.",
          "type": "string"
        },
        "multipart": {
          "description": "Override body as multipart/form-data with fields and files.",
          "type": "object"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "removeHeaders": {
          "description": "Header names to remove from the replayed request.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "requestId": {
          "description": "Required. Request ID from profile_traffic_query to replay.",
          "type": "string"
        },
        "url": {
          "description": "Override the request URL.",
          "type": "string"
        }
      },
      "required": [
        "requestId"
      ],
      "type": "object"
    }
  },
  {
    "description": "Replay one captured managed browser request through multiple variants and return response diffs for edit-and-resend security testing.",
    "name": "profile_request_replay_batch",
    "parameters": {
      "properties": {
        "maxBodyPreview": {
          "description": "Max characters of body diff preview per variant.",
          "type": "number"
        },
        "maxVariants": {
          "description": "Cap on number of variants to replay. Default: unlimited.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "requestId": {
          "description": "Required. The requestId to replay (from profile_traffic_query). Use requestId (singular), not requestIds.",
          "type": "string"
        },
        "variants": {
          "description": "Required. Variant override objects; each may override url/method/headers/body etc.",
          "items": {
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "requestId",
        "variants"
      ],
      "type": "object"
    }
  },
  {
    "description": "Recover a durable profile after an agent chat/session or browser tab was closed. Reuses a live tab when attached; otherwise reopens the profile's last URL in a fresh managed tab and binds it back.",
    "name": "profile_resume",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Required. Profile name to resume.",
          "type": "string"
        },
        "reload": {
          "description": "Force reload even when the tab is already attached. Default: false.",
          "type": "boolean"
        },
        "url": {
          "description": "URL to open if the profile has no live tab. Defaults to the profile's last URL.",
          "type": "string"
        },
        "waitMs": {
          "description": "Wait time in ms after navigation. Default: 800.",
          "type": "number"
        }
      },
      "required": [
        "profile"
      ],
      "type": "object"
    }
  },
  {
    "description": "Export profile-local managed browser network traffic as a HAR-like file and return the saved path.",
    "name": "profile_save_har",
    "parameters": {
      "properties": {
        "includeBodies": {
          "description": "Include response bodies in the HAR. Default: false.",
          "type": "boolean"
        },
        "limit": {
          "description": "Max traffic records to export. Default: 1000.",
          "type": "number"
        },
        "maxBodyBytes": {
          "description": "Max bytes per response body when includeBodies=true. Default: 200000.",
          "type": "number"
        },
        "path": {
          "description": "Absolute path for the HAR file. Defaults to evidence/har/<timestamp>-network.har.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Get one captured traffic record for a managed profile. If profile is omitted, uses the server default profile.",
    "name": "profile_traffic_get",
    "parameters": {
      "properties": {
        "profile": {
          "description": "Profile name. Defaults to server default profile.",
          "type": "string"
        },
        "requestId": {
          "description": "Required. Request ID from profile_traffic_query.",
          "type": "string"
        }
      },
      "required": [
        "requestId"
      ],
      "type": "object"
    }
  },
  {
    "description": "Query captured network traffic for a managed profile. If profile is omitted, uses the server default profile.",
    "name": "profile_traffic_query",
    "parameters": {
      "properties": {
        "failed": {
          "description": "Filter: if true, return only failed requests.",
          "type": "boolean"
        },
        "from_cache": {
          "description": "Filter: if true, return only cache-served responses.",
          "type": "boolean"
        },
        "from_service_worker": {
          "description": "Filter: if true, return only service-worker-handled responses.",
          "type": "boolean"
        },
        "has_request_body": {
          "description": "Filter: if true, return only requests that have a captured request body.",
          "type": "boolean"
        },
        "has_response_body": {
          "description": "Filter: if true, return only requests that have a captured response body.",
          "type": "boolean"
        },
        "hostname": {
          "description": "Filter: return only requests to this hostname.",
          "type": "string"
        },
        "limit": {
          "description": "Maximum results to return. Default 50.",
          "type": "number"
        },
        "method": {
          "description": "Filter: return only requests with this HTTP method (e.g. \"GET\", \"POST\").",
          "type": "string"
        },
        "mime_contains": {
          "description": "Filter: return only responses whose MIME type contains this substring.",
          "type": "string"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile if omitted.",
          "type": "string"
        },
        "redirected": {
          "description": "Filter: if true, return only redirected requests.",
          "type": "boolean"
        },
        "request_header": {
          "description": "Filter: return only requests containing this header (key-value object, e.g. {\"Authorization\": \"Bearer\"}).",
          "type": "object"
        },
        "resource_type": {
          "description": "Filter: return only requests of this resource type (e.g. \"XHR\", \"Fetch\", \"Document\").",
          "type": "string"
        },
        "response_header": {
          "description": "Filter: return only responses containing this header.",
          "type": "object"
        },
        "sort_by": {
          "description": "Field to sort results by (e.g. \"timestamp\", \"status\", \"url\"). Default: timestamp.",
          "type": "string"
        },
        "sort_dir": {
          "description": "Sort direction. Default \"asc\".",
          "enum": [
            "asc",
            "desc"
          ],
          "type": "string"
        },
        "status": {
          "description": "Filter: return only requests with this exact HTTP status code.",
          "type": "number"
        },
        "status_max": {
          "description": "Filter: return only requests with status <= this value.",
          "type": "number"
        },
        "status_min": {
          "description": "Filter: return only requests with status >= this value.",
          "type": "number"
        },
        "url_contains": {
          "description": "Filter: return only requests whose URL contains this substring.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Summarize profile-local managed browser Network events for agent dashboards and triage.",
    "name": "profile_traffic_summary",
    "parameters": {
      "properties": {
        "limit": {
          "description": "Maximum entries to include in the summary sample. Default 10.",
          "type": "number"
        },
        "profile": {
          "description": "Profile name. Defaults to the server default profile if omitted.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  {
    "description": "Copy Google trust cookies from the user's personal Chrome into a managed profile. This makes the managed profile appear as a real user to bot detection systems (Arkose Labs, reCAPTCHA) without sharing any target-site cookies. Call this before registering an account or visiting a page that uses bot detection on a fresh managed profile.",
    "name": "profile_warm_from_personal",
    "parameters": {
      "properties": {
        "domains": {
          "description": "Domains to copy from personal Chrome. Defaults to Google trust domains: .google.com, .accounts.google.com, .gstatic.com.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "profile": {
          "description": "Managed profile name to warm. Defaults to the active default profile.",
          "type": "string"
        }
      },
      "type": "object"
    }
  }
]
